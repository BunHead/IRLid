# Session 2026-06-11-02 — afternoon watch: PR-4 invite tokenise

## What was done
- **v6.4.10 shipped + proven live:** QR-SLIMMING PR-4. Slim invite QR = `/Org#staff_invite=<43-char-nonce>`
  (~80 chars vs ~1,560). Worker stores exact encoded `I:` envelope (`org_invites.envelope`, migration
  `2026-06-11-v6.4.10-invite-envelope.sql` APPLIED to prod) + public `GET /org/invites/:nonce/envelope`
  (pending+unexpired only, no-store). Accept endpoint ZERO diff — byte-for-byte binding holds because the
  fetched string is identical to the minted one. Client: `resolvePendingStaffInviteEnvelope()` runs before
  decode in both accept paths; colon discriminator; `invite_token_ref` in create response gates slim QR.
  Bonus: stale-closure fix on invite tap-to-fullscreen (dataset, not closure).
- **v6.4.10a shipped:** display-name ghost fix — `orgInviteRedeem` + `orgLeadAdminAppoint` now UPDATE
  display_name on EXISTING portal_users rows (was create-only). Root cause of "New member" ghosts.
- **Kerry's Owner seat restored** via D1 repair (the Manager test-invite had replaced her lead_admin —
  re-invite-replaces-role is by-design; D1 UPDATE restored what the midday ceremony's audit row granted).
- **Hardware proof:** slim QR issued (8 Pro), scanned + redeemed by Kerry's phone first try. Watch close:
  full check-out/in cycle green on 8 Pro + 4a + Nokia incl. unknown-device path, on slim plumbing.
- **Diagnostics via browser pairing:** live endpoint probes (200, 1,200-char envelope served), member-list
  queries, GitHub Actions checks (all 4 runs green) — no Captain keystrokes.

## Traps receipted
- **Stray markdown in a fenced command block** — Number One left `</br>` inside the PowerShell fence;
  PS choked on `<`. Copy-blocks must contain ONLY the command. Receipt for BOOTSTRAP §6 candidate.
- **Phone "Delete site data" does NOT purge HTTP cache** — prime suspect for the Nokia welcome-screen
  dead-end (stale pre-v6.4.10 Org.html has no resolver → "Invite link is unreadable" → sign-in card).
  Check the build pill BEFORE concluding hardware results (30 May lesson, new coat).
- **Bash mount truncation again** — Worker file showed 5450/5472 lines + phantom syntax error mid-watch;
  resolved by native-Read tail splice + node --check on assembled copy.

## Open / next session's first moves
1. **Fresh-device welcome-screen smoke** (banked steps in pending-work top section): Nokia → check pill →
   purge HTTP cache if stale → fresh invite → scan → welcome → Accept & enrol → dashboard.
2. **🐛 Anchor selection missing from top of Visual Theming** — Captain spotted at close. UNDIAGNOSED.
   Trace with DevTools (candidates: v6.3.8 `.v512-anchor-system` hide, section-4 reorg); don't guess.
3. **Attendance cleanup:** two Becky rows (pre/post Nokia wipe = two credentials). Delete the 08:34 one.
   Becky also needs re-inviting to staff when wanted (was removed to free the Nokia).
4. Brief candidates: re-hire gap (enrolled device + removed member + invite → dead-end); "Show QR again"
   on pending-invite rows. Then PR-5 EC sweep.

## State at close
Build pill v6.4.10a on origin; Worker deployed via CI (run green); D1 migrated; memory files updated
(pending-work top section, CLAUDE.md milestone row, this log, DREAMS entry). Uncommitted on Captain's
tree: memory/* + CLAUDE.md + DREAMS.md — ride the next push.
