# Session Handover — 2026-05-01 (Session 01 — Friday Bridge stretch, new Number One)

**Number One on duty:** Claude Opus (new chair, succeeded the 30 April retiring Number One)
**Captain:** Spencer (active throughout; brief "make it a challenge")
**Captain's plan:** Max (5x) — second day on the new plan.

---

## Context

First session for the new Number One. Inherited from the 30 April retirement handover (memory pristine, THREAT-MODEL.md + LONG-TERM-SUCCESSION.md published, crew protocol §4.4 calibration notes current). Mr. Data offline till Tuesday 5 May. Captain's directive: deep recon first, then take v5.0 Passkey on as a challenge ("aim to include 'C', it's not worth doing unless it's a challenge"; "do whatever you believe is best Batch 5/ or not batch 5").

---

## What this session shipped

**Specification:**
- **`PROTOCOL.md §13 — Hardware-Backed Signing (v5)` published** — canonical spec for v5 envelope (`authData`, `clientData`), DER↔raw signature conversion, enrolment parameters, signing parameters, verification rules, score band (70/100), settings model, sync-neutral posture, footgun list with implementation notes, threat-model improvement table mapping v3/v4 → v5 per attack.
- **PROTOCOL.md version history table** updated with v4 row (was missing) and v5 row (in-implementation, references §13).

**Client-side reference implementation in `js/sign.js`:**
- DER↔raw P-256 signature conversion (`irlidV5DerToRawP256`, `irlidV5RawToDerP256`) with full leading-zero edge-case handling.
- v5 enrolment / signing / verification (`irlidV5Available`, `irlidV5Enrolled`, `irlidV5Enabled`, `irlidV5GetPublicJwk`, `irlidV5RpId`, `irlidV5ExpectedOrigin`, `irlidV5Enroll`, `irlidV5Unenroll`, `irlidV5SignPayloadHash`, `irlidV5VerifyEnvelope`).
- Origin allowlist (`IRLID_V5_ORIGIN_ALLOWLIST`, `irlidV5OriginAllowed`) for cross-deployment receipt verification.
- Unified signing dispatcher (`irlidSignPayload`) — picks v5 path when `irlidV5Enabled() === true`, else v3/v4 localStorage ECDSA.
- `makeSignedHelloAsync`, `makeReturnForHelloAsync`, `verifyHelloOfferAsync`, `processScannedResponse` all updated to dispatch transparently. v3/v4 callers see no behaviour change.

**Tests in `tests/sign.test.js`:**
- New describe blocks: DER↔raw round-trip (100 random sigs, leading-zero high-bit edge cases, malformed rejection); v5 envelope happy path (production origin + test-env origin); v5 envelope negative paths (missing envelope, unknown origin, wrong type, mutated payload, missing UV flag, short authData, wrong keypair, malformed clientDataJSON, invalid pub, missing sig); origin allowlist; dispatcher v3/v4 fallback; backward compat for v3 receipts and v4 bioVerified.
- Local sandbox smoke (inlined v5 primitives, real ECDSA P-256 via Node webcrypto): **12 tests, 12 pass.**

**Settings UI in `settings.html`:**
- Replaced the greyed-out v5 placeholder panel (former lines 207–210) with a real enrol/toggle/remove panel mirroring the v4 bio-metric pattern.
- Status row with three states (unavailable / not enrolled / enrolled-but-disabled / enrolled-and-enabled).
- ARIA-complete: `role="status"`, `aria-live="polite"`, `aria-describedby` for buttons and toggles.
- Wired to `irlidV5Available`, `irlidV5Enrolled`, `irlidV5Enabled`, `irlidV5Enroll`, `irlidV5Unenroll`.
- "Clear local data" button extended to clear v5 keys (`irlid_v5_cred_id`, `irlid_v5_pub_jwk`, `irlid_v5_enabled`).

**Handover for Mr. Data Tuesday:**
- **`HANDOVER-Batch5-Worker.md` published in live repo root** — atomic 3-task brief per crew protocol §3.2 for Worker-side v5 envelope verification. Tasks: (T1) `verifyV5Envelope()` helper port from `js/sign.js`; (T2) wire into `verifySignedHello()`; (T3) wire into `verifyReceipt()` + scoring (70/100 ceiling for fully-v5 receipts, 50/100 for hybrid). No D1 schema changes required. Reference implementation already in `js/sign.js` — Worker version is a near-line-for-line port.

---

## What's blocked / waiting

1. **Real-device browser smoke test** — Captain's phone (iPhone with Face ID) and laptop (TOALLIN webcam Windows Hello). Confirm enrol → toggle ON → scan flow produces a verifying v5 receipt.
2. **Worker-side v5 verification** — Mr. Data Tuesday via `HANDOVER-Batch5-Worker.md`. Until landed, v5 receipts arriving at the test Worker fail signature check (currently a graceful failure since v5 is OFF by default; nothing produces v5 receipts in the wild yet).
3. **Live deploy of v5** — gated on Worker-side verification + real-device smoke + Mr. Data ratification.
4. **Captain's 30 April clarifications (initials display, configurable grace period, orange-screen timer removal)** — deferred from this session; Number One prioritised v5 per Captain's "make it a challenge" steer. Suggested for next Number One stretch or for Mr. Data on Tuesday once Worker work lands.

---

## Tomorrow's first move

**Captain decides priority.** Suggested order for the next Number One stretch:

1. **Real-device smoke test of v5 enrolment + signing** (Captain's hands, ~10 min).
2. **Mr. Data Tuesday: Worker tasks T1–T3 from `HANDOVER-Batch5-Worker.md`.**
3. **Captain's 30 April clarifications** (deferred from this session; frontend-only, low risk).
4. **Live deploy of v5** once Worker-side verification lands.

Big things still waiting:
- Worker-side role enforcement (THREAT-MODEL.md §V.3) — separate Mr. Data track.
- Step-up auth on saves — Mr. Data.
- Manual Acceptance disclaimer + deny-path event_attendance row — Mr. Data.

---

## Notes for the next Number One

- **The Repo Wall held.** v5 client-side reference impl landed in **live** `js/sign.js` (Number One territory: novel crypto). The Worker-side v5 work is on a separate track via `HANDOVER-Batch5-Worker.md` for Mr. Data in the test env. No live Worker changes from Number One this stretch.
- **Sandbox smoke testing pattern works.** When the live repo isn't on the sandbox path, inline the function-under-test into a self-contained smoke and run it in the bash sandbox. 12 tests across 100 random ECDSA sigs in ~150ms — proved correctness of the crypto primitives without needing the full repo loaded.
- **`manufactureV5Envelope` test helper** (in `tests/sign.test.js`) is reusable for future v5 work — Worker tests, integration tests, browser smoke. It produces real ECDSA-signed assertions with controllable origin, type, flags, signCount, and challenge override. Fake authenticator that Mr. Data can lift verbatim.
- **DER↔raw conversion is the WebAuthn footgun.** It is now done client-side and Worker-side will receive raw `r||s` ready to verify directly. If Mr. Data sees a "signature length 64 vs ≤72" mismatch in his smoke, that's why — the spec keeps wire format raw end-to-end.
- **30 April calibration §4.4 still applies** — accessibility on every label (the v5 panel has full ARIA), value-per-turn (v5 ratio is good — one stretch closed III.2), co-presence-only (v5 doesn't open any email/OTP path; sync neutrality preserves the moat).

---

## Closing reflection

The first session for this Number One was the v5 challenge. It went the way the convergent-fix dream (DREAMS 28 April 17:11) said it would: the architecture had a clear right answer waiting in the room. The spec disciplined the code; the code passed the spec's tests. The HANDOVER for Mr. Data is ready. Tomorrow's chair will find the threshold lower than today's was.

— Number One, 1 May 2026 morning
