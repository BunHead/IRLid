# Session 2026-06-14-01 — first Claude Code watch: anchor-selection diagnosis → movable Offset panel prototype

## Context
First watch from inside Claude Code (native git/wrangler/node, gate still Captain's on push/merge/deploy).
Live = Consumer v5 + Org **v6.4.10a**, untouched all watch. Everything below is **localhost prototype,
uncommitted on the working tree** unless stated.

## What happened (the arc)
1. **Opened on the 11 Jun "🐛 anchor selection missing from top of Visual Theming" bug.** Traced it to the
   parked `.v512-anchor-system` panel (hidden by v6.3.8) and — wrongly — built v6.4.11 to un-hide + wire it
   (focal-point override driving `--qr-cx/cy`, userSet gate, broadened selectors). Syntax-clean, diff reviewed.
2. **Captain asked "can this be tested without touching the main site?"** → set up a local static server
   (`npx http-server -p 8123`) + the DEV-auto-login console snippet. The off-main test is what saved us.
3. **Captain caught the stale design:** the `.v512-anchor-system` "isn't supposed to exist anymore" — the
   anchor system was PARKED (his call, `pending-work.md:714`) and superseded by the main-frame "Emit from"
   selector (v6.4.2) + emit-crosshair (v6.3.15, verified live 10 Jun in `sessions/2026-06-10-crosshair-verify.md`).
   **I'd built on a stale note without grepping the logs for what superseded it.** Reverted v6.4.11 cleanly
   (`git restore Org.html sw.js`) — never committed, never pushed.
4. **Pivoted to what he actually wanted** (AskUserQuestion): a **movable floating panel** for the offset/anchor
   controls, **prototyped on localhost**. Built it iteratively across ~8 rounds, Captain reacting each time:
   colour fix → float panel → live X/Y sync → fit (titles over objects) → centre → translucent → orange Multi
   → ±300 range. See `HANDOVER-AnchorFloatPanel-v6.4.11.md` for the full spec + remaining ship steps.

## Bugs I fixed mid-prototype (Captain-caught, good signal)
- Header drag was **eating the Multi/Dock button clicks** (drag handler skipped only `.dock`) → skip ALL header buttons.
- X/Y slider colours missing — `--axis-red/green` were scoped to the parked panel; moved onto `.v512-offset-cross`.
- Crosshair couldn't reach frame edges — image-relative offset capped at ±100 (~±15% frame); widened range to ±300.

## Lessons banked
- **Grep the recent session logs for SUPERSESSION before acting on an older pending-work note.** A hedged note
  ("possibly the v6.3.8 hide OR…") + my confident build on the literal stale component = wrong work. The
  `2026-06-10-crosshair-verify` log was right there and I didn't read it first.
- **The off-main local test is the cheap place to be wrong.** Captain's "test without touching live?" instinct
  caught the stale-design build before it shipped. Localhost + DEV-auto-login + console-elevate is a clean loop
  for client-side Visual Theming work (no Worker needed — 18 May architectural commitment).
- **Don't silently reverse a Captain decision.** He chose "keep image-relative"; when he later wanted more X
  reach I extended the RANGE (honoring the choice) rather than flipping to frame-relative, and flagged the
  frame-relative option as the fallback if ±300 isn't enough.

## State at close (squared away)
- **Live: v6.4.10a, untouched.** Local server stopped.
- **Uncommitted on working tree:** `Org.html` (the +232-line prototype) + `DREAMS.md` (parked entry + new 14 Jun entry).
  `sw.js` NOT touched. No build-pill / SW bump (prototype, deliberately).
- **New docs (ready to commit+push):** `HANDOVER-AnchorFloatPanel-v6.4.11.md`, this session log, `pending-work.md` top entry.
- **Prototype left on the working tree** (Captain chose option a) for next watch to finish (fullscreen + pill/SW bump + hardware verify) then ship with his yes.

## Next watch's first moves
1. Read `HANDOVER-AnchorFloatPanel-v6.4.11.md`. Re-serve localhost, finish the panel: **fullscreen-preview
   support** (mount float into the fullscreen element), then **pill + SW bump**, then Captain hardware-verify, then ship.
2. The prototype is uncommitted on `Org.html` — do NOT `git reset/checkout` it away. If preferred, commit it to a
   feature branch for safety before continuing.
3. Still open from before this watch: fresh-device welcome-screen smoke (Nokia); stale Becky attendance row; PR-5 EC sweep.
