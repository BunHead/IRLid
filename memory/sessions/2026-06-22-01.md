# Session 2026-06-22-01 — demo-polish marathon (v6.4.15 → v6.4.19), bug hunt, LiDAR banked

First Claude Code watch since 16 Jun. Captain opened asking for: site status, whether to make the "logs
button" work, and a web scan for external IRLid mentions. It turned into a full demo-readiness pass plus
three real bug hunts driven by Captain's live hardware use.

## What was done (all LIVE on production)

- **v6.4.15 / v6.4.15a — Org audit log (the "logs button" is now real).** Settings → Tools & Diagnostics
  "Open log" opens an append-only authorization log (who authorised what, when, allowed/denied). New
  immutable `org_audit_log` table; `GET /org/audit-log` (lead_admin+/developer); best-effort `recordAudit`
  hooks on signed actions (allowed + insufficient_role denials), lead-admin appoint (batched), invite
  create/accept/redeem, member remove, settings save. Hardware-verified — Captain's calendar change showed
  as a row with his name. **15a fixes:** (1) endpoint auth widened to accept the bootstrap-developer
  api_key Bearer (`bootstrapDeveloperFromBearer`) not just strict `requireSession` — first smoke gave
  session_invalid/403; (2) the `--file` migration hit a **wrangler-OAuth-token D1-scope wall**
  (`Authentication error 10000` — token has workers/workers_kv write, NO d1), so the **Worker now
  self-creates the table** via `ensureAuditTable(env)` (its D1 binding has write access regardless of the
  local token). Migration file kept as schema doc; no longer required.
- **v6.4.16 — session-expiry bounce.** A lapsed 24h login session made every session-gated action throw
  `session_invalid` with no recovery (the v5.10.7 heartbeat never got ported into capital Org.html). Now
  `orgapi.js` + inline `liveCalendarApi` dispatch `irlid:session-expired` on any 401 session_*, and
  `Org.html handleSessionExpired()` (debounced) signs out + prompts re-login + a 30s heartbeat catches idle
  expiry. Localhost-verified (dispatch → clean bounce to sign-in card).
- **v6.4.17 — dead-button sweep + Patreon unify.** Audited all 486 dashboard buttons (source analysis):
  only 1 id'd dead button (`signInHereBtn`, but `hidden` = not a dead click; left as-is). Disabled the 3
  design-in Records & ID placeholders (Connect storage / Add custom field / Open RTBF helper) to match the
  IN-DESIGN disabled pattern; WIRED the Sign-in & Auth sign-out buttons (this device → signOutOrg; all
  devices → IRLidOrgApi.signOutAllDevices — had NO other UI entry). Unified Patreon URL to
  `patreon.com/c/IRLid` (login.html + contact.html; forums.html already correct — Captain confirmed via his
  creator dashboard). Localhost-verified.
- **v6.4.18 (Worker-only) — Delete-expected gate fix.** "Delete expected" failed with `signed_action_required`:
  `orgExpectedDelete` was gated with `requireCalendarSignedAction` (needs a per-action WebAuthn envelope the
  UI never sends), while the inverse op `orgExpectedCreate` uses `requireCalendarStaff` (staff/dev Bearer).
  Swapped delete to `requireCalendarStaff` — add + remove now share one gate. Hardware-confirmed ("Expected
  attendee removed", Becky gone, first-click).
- **v6.4.19 — audit-board ROLE column.** `.role-mini.inverted.attendee` set the letter to `var(--muted)` —
  the SAME colour as the inverted fill — so the "A" was invisible on every checked-IN attendee (the whole
  audit board read as blank role circles). Now dark `var(--bg)`, verified `sameColour:false` (letter
  rgb(13,17,23) on fill rgb(139,148,158)).
- **LiDAR banked into the forward plan.** PROTOCOL.md §10.3 gained an `anchors[]` type `lidar-geometry`
  (v8.0 drone tier) + a design note: LiDAR turns location from *asserted* (GPS, spoofable) into *measured*
  (a depth map; a photo of a marker is flat in a point cloud). Attests geometry not identity → complements
  the cryptographic who/when. **Gated on Wisdom** — ASE Tech's public site (asetech.co.uk) lists Badger
  (VTOL) + Mantis (quad) + S.W.A.R.M. + "onboard processing" but NO published sensor loadout; confirm which
  airframes carry depth sensing. Also in pending-work.
- **Web scan (Captain asked):** beyond irlid.co.uk itself (homepage + the proof-of-personhood blog, both
  indexed + accurately summarised), **no third-party footprint** found (no Reddit/PH/HN/dev.to). Caveats:
  the search tool is US-indexed, Reddit blocks indexing, and the April promo aged out. Net: no organic
  external conversation yet — which matches the North Star (promo only when Captain judges it ready).

## Bugs found this watch

1. **Session-expiry no-bounce** — FIXED (v6.4.16).
2. **Delete-expected over-gated** — FIXED (v6.4.18).
3. **Offline write queue silently DROPS signed-action mutations** — BANKED, not fixed. `/org/expected` is in
   `QUEUE_ELIGIBLE_PATHS`; a network blip enqueued the bodyless DELETE; on replay the Worker 400s (needs a
   signature) → `quarantine()` drops it silently → user thinks it synced, action vanished. Post-demo brief:
   exclude signed-action endpoints from the queue, OR surface dropped ops to the user. Diagnosed live via
   Chrome-MCP on the shared IndexedDB queue.

## Receipts state (Captain asked "do receipts show on the site yet?")

YES — Receipt Bridge is live. Org check-in mints a signed receipt; dashboard "↓ Receipt" link
(`buildReceiptLink`, Org.html ~15538) → `receipt.html?org_receipt=<id>` (renders) + check.html (verifies).
`org_receipt` handled in receipt.html + check.html + org-entry.html. **Gap:** no "browse all receipts"
history view — per-record via URL only. A receipts-history page is a small build if Captain wants a demo
"moment" around it.

## State at watch close

- All of v6.4.15 → v6.4.19 deployed + verified live. Both pushes confirmed (`…4458577` = v6.4.19).
- Attendance board cleaned (Becky / Poppy / Someone Else / Yet Another stale "expected" rows deleted).
- DREAMS.md has a new entry ("the innocent deploy × the clean room and the real hand × IRL debugging
  IRLid") — UNCOMMITTED in the working tree (Captain lands DREAMS when he likes).
- Forum welcome-post draft written to `FORUM-WELCOME-DRAFT.md` (root) — awaiting Captain's review tomorrow.
- This session log + the successor letter are new (uncommitted) — land with a memory commit whenever.

## Next session's first move (Captain's stated plan: 1 & 2 first thing)

1. **Full demo dress-rehearsal on hardware** — doorman orange→green, check-in/out cycle, audit board,
   celebration, receipt link. (Today's bugs all surfaced from real use; a deliberate run catches the rest.)
2. **Receipts polish** — clean up the receipt presentation; decide if a browsable receipts-history view is
   worth building for the demo.
3. **Forum welcome post** — Captain reviews the `FORUM-WELCOME-DRAFT.md` draft; he posts + pins it.
4. **Offline-queue silent-drop fix** (the real banked bug).
5. **Records & ID inert-inputs tidy** (finish the dead-control sweep — disable the design-in inputs).
6. Promo prep (drafted, not posted — Captain-gated); Discussions welcome post; land DREAMS.md.

## Methods banked

- **Chrome-MCP on Captain's own live browser** was decisive for diagnosis — probed his real session
  (`/org/members` → 401 = stale session token, api_key fine), and read the **shared IndexedDB offline
  queue** from a side tab (listAll → the dropped DELETE; replay → console "HTTP 400"). Live-state inspection
  beat reasoning every time this watch.
- **Claude Preview static-serve** (`.claude/launch.json`, `npx serve`) for localhost frontend verification
  (button-opens-dialog, session-bounce, role-letter computed-colour). Note: doesn't run the Worker — Worker
  changes verify only on Captain's hardware.
- **Worker self-creates its table** from the D1 binding when the local wrangler token lacks d1 scope —
  idempotent, deploy-order-safe, dodges the "migration never stamped on prod" trap. Reusable pattern.
