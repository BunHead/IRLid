# Session Handover — 2026-05-04 (Session 01 — Monday long-watch, dawn through afternoon)

**Number One on duty:** Claude (Sonnet 4.6, Cowork mode), commissioned ~07:00 BST after Captain's "It's just before 7am and it'd appear that the reddit post has not landed at all :(" opener.
**Pronoun:** he/him this watch.
**Captain:** Spencer.
**Captain's plan:** Pro tier (steady from yesterday).

---

## Context

Inherited Sunday's freshly-shipped Theming v6.5f system + the v5 production stack. Day's brief, in retrospect: ship the entire identity-bound-sessions layer (§14) including schema, Worker endpoints, frontend QR-scan login, and the create-org flow. Bootstrap end-to-end with hardware-backed signing. Test env only. **Achieved.**

This is the day IRLid started using its own protocol on itself.

---

## What this session did

**Morning (07:00 → ~09:00 BST) — recon + tidy + pivot to v5.5:**

- Acknowledged previous evening's cym13 post hadn't landed; Captain elected to defer that thread.
- Three deferred follow-ups from Batch 6.5g shipped: legacy `.toggle-row` Quick Settings card removed (full saveSettings/togToggle trace before deletion); `renderIdentityPicker` now branches on `IRLID_FIRST_SCAN_FLAGS.allowSelfSelection` (false → "See an organiser" route); `triggerAcceptCycleAnimation()` hooked into `manualCheckin` success/walkin paths so successful check-ins fire the celebration on real-flow.
- Captain raised the org sign-in question. Recon revealed the existing flow was paste-an-`org_<key>` only — no human identity, no recovery, single shared secret per org. Captain articulated the vision: scan a QR with your phone, signed-HELLO recognises you, log you in with role-appropriate permissions. First-time / new-org case: only lead_admin / developer can create new orgs, with org name + staff/manager scan-ins + website URL → auto-extract logo & theme.
- Number One's response: yes possible, fits the architecture beautifully — recursive in the cleanest way (the protocol's primary primitive is "this device with this private key was here," and we're applying that primitive to "this device is the human signing in"). Phased plan A→B→C→D in PROTOCOL.md §14.13. Bootstrap path: hardcoded developer pub_fp env var (Captain's phone) for v5.5; invite-token fallback for v5.6+.

**Spec landed first (~09:00 → 10:00 BST):**

- `PROTOCOL.md §14 Identity-Bound Sessions` (~250 lines) published. Storage model, login flow ASCII diagram, login QR format, bootstrap mechanism with rotation pattern, opaque-session-token-not-JWT justification grounded in IRLid principles (revocation legibility over stateless convenience), endpoint contracts for `/org/login/*` + `/user/*`, role permission matrix (`attendee/staff/manager/lead_admin/developer` — Captain corrected `supporter` → `attendee` in initial spec), threat model (10 rows including cross-context envelope replay defence via `type` discriminator, malicious-worker-URL allowlist, brute-force claim rate limiter, source-IP correlation note), backward compat (api_key retained for service accounts), §14.13 phasing, §14.14 scaling path (long-poll → SSE → Durable Objects with endpoint contracts unchanged through all three tiers). Version History table updated with v5.5 row.

**Batch A — schema + login endpoints (~10:00 → 11:30 BST):**

- `apply_batch_a_identity_sessions.ps1` migration created `portal_users`, `org_memberships`, `login_sessions`, `login_challenges` tables.
- Initial run failed: `users` table name collision with the OAuth `users` table from `/auth/register` endpoints. Patch: renamed to `portal_users` throughout migration + Worker SQL + spec FK references.
- Worker: `verifyV5Envelope()` ported from live Worker (test Worker had been missing it despite an earlier session-log claim — surfaced and corrected); `orgLoginInit` / `orgLoginPoll` / `orgLoginClaim` endpoints with rate limiter (3 fails per nonce → 5min cooldown) and §14.10 generic `auth_failed`. Bootstrap recognition off `env.BOOTSTRAP_DEVELOPER_FP` Cloudflare secret.
- Captain set the secret via `wrangler secret put` — first attempt timed out (Cloudflare API was flaky most of the day, multiple retries needed for almost every wrangler operation).

**Batch B — phone-side login page + admin-portal sign-in rebuild (~11:30 → 13:00 BST):**

- Live repo: `org-login.html` published. Mobile-first page reads `?nonce=<n>&worker=<workerUrl>`, validates worker URL against allowlist (defence in depth — even though a captured-and-replayed envelope would be useless to an attacker because they can't insert their nonce into the legitimate Worker's challenge table, the allowlist refuses outbound POSTs to non-IRLid hosts entirely). Signs canonical `{nonce, type:"irlid_login_v5"}` payload via `irlidV5SignPayloadHash()`. Diagnostics panel for debugging.
- Test repo: `OrgCheckin.html` panel-setup rebuilt — primary path is "Show login QR" + polling display, legacy register/key-paste demoted into `<details>` "Service-account login" expander. `?dev=0` URL escape hatch added to defeat `DEV_AUTO_LOGIN` for testing the real flow.
- `js/orgapi.js`: added `loginInit()`, `loginPoll(nonce)`, `workerBaseUrl()` wrappers.
- Worker payload verifier expects `{nonce, type:"irlid_login_v5"}` (type discriminator binds login-context envelope against cross-context replay).

**Three follow-up patches to Batch B:**

1. CORS allowlist extended to `https://irlid.co.uk` — Worker's `corsHeaders()` previously hardcoded `bunhead.github.io` only via `env.CORS_ORIGIN`. The login flow is cross-origin by design (org-login.html lives on irlid.co.uk, POSTs to workers.dev), so this entry was structurally required.
2. Verbose-debug login errors — replaced §14.10 generic `auth_failed` with rich `debug_reason / debug_computed_fp / debug_bootstrap_fp_first4 / debug_bootstrap_fp_last4` fields temporarily so Captain could pinpoint failures during smoke testing. Marked as test-env-only with a TODO to gate behind env flag for production.
3. Login challenge TTL 60s → 180s — IRL test showed 60s left zero margin once camera-app QR scan + page load + biometric prompt + sign + POST overhead were accounted for. 180s still bounds replay window narrowly.

**Bootstrap secret recovery (~13:30 BST):**

- Verbose-debug surfaced the actual failure mode on attempt 4 of the smoke test: `debug_bootstrap_fp_len: 1, debug_bootstrap_fp_first4: "", debug_bootstrap_fp_last4: ""` — Captain's `wrangler secret put` had been pasting Ctrl+V at the PowerShell secure-input prompt; PowerShell registered a single literal Ctrl+V keypress (ASCII 0x16, "Synchronous Idle") rather than performing a clipboard paste, so the secret was set to one byte.
- Recovery: `"TvklFsivZk68R67j" | wrangler secret put BOOTSTRAP_DEVELOPER_FP` (pipe stdin) preserves the value cleanly. Worker's `(env.BOOTSTRAP_DEVELOPER_FP || "").trim()` handles trailing newline.
- This is exactly the failure mode the verbose-debug patch was designed to expose, and it earned its keep on first use. Logged in CLAUDE.md milestones for future reference.

**The bootstrap login worked end-to-end (~14:00 BST). 🟢**

- Captain's Pixel 8 Pro v5 hardware credential signed the login challenge on `irlid.co.uk/org-login.html`.
- Phone POSTed `{nonce, pub_jwk, sig, webauthn:{authData, clientData}}` to `irlid-api-test.irlid-bunhead.workers.dev/org/login/claim`.
- Worker `verifyV5Envelope()` recomputed canonical hash of `{nonce, type:"irlid_login_v5"}`, matched `clientData.challenge`, asserted UV flag, ECDSA-verified the signature.
- `deviceKeyFp(pub_jwk)` produced `TvklFsivZk68R67j`; matched `BOOTSTRAP_DEVELOPER_FP`; created founding `portal_users` row with `display_name: "Captain (developer)"`.
- Issued 32-byte opaque session token with 24h sliding TTL into `login_sessions`; bound session to challenge.
- Phone displayed "Signed in. You may close this tab." Desktop poll picked up claimed status, displayed "Signed in as Captain (developer). No orgs yet — the create-org flow ships in Batch C." Captain: *"Think we have a pulse Jim ;) ;)"*

**Batch C shipped (~14:30 → 15:30 BST):**

- Worker: `requireSession()` Bearer-token auth helper (sliding 24h TTL), `userListOrgs()` returning user's memberships with role + api_key (api_key included so legacy X-Org-Key dashboard code keeps working through v5.5 transition), `userCreateOrg()` validates name/website_url, checks authorisation (developer pub_fp OR existing lead_admin/developer membership), creates organisation row + venue keypair + `org_memberships` row.
- Frontend: `handleQrLoginSuccess` rewritten async. On `orgs.length === 0 && can_create_org`, reveals create-org form; on orgs ≥ 1, fetches `/user/orgs` for api_keys then auto-loads dashboard via new `loadDashboardForOrg()`. New create-org form section in panel-setup with org name + optional website URL.
- Staff scan-in flow deferred to Batch C.5 (mirror of bootstrap login pattern). Website scrape deferred to Batch D.

**Afternoon — GitHub Pages stuck (~15:30 onwards):**

- After Batch C deploy, browser still showed pre-Batch-C UI despite hard refresh.
- Diagnostic chain: PowerShell Select-String confirmed local file had Batch C content; `git status` reported clean working tree; `curl raw.githubusercontent.com` showed origin had Batch C; `curl bunhead.github.io` did NOT show Batch C → Pages CDN was serving stale content.
- GitHub Actions investigation revealed: the `Login challenge TTL 60s -> 180s` commit (`3e3321e5...`) became stuck mid-deployment in GitHub Pages' pipeline and never completed. Pages serialises one deployment at a time per environment; subsequent deployments (Batch C, renormalize) all failed with `Deployment request failed... due to in progress deployment. Please cancel 3e3321e5... first or wait for it to complete.`
- Re-run of #108 (renormalize) submitted; queued as of session pause.
- Captain pivoted to documentation work (this file) while Pages drains.

---

## Partnership moments worth logging

**Captain pushed back constructively.** When the OAuth `users` table collided with my §14 `users`, when the Ctrl+V paste mangled the bootstrap secret, when the IRLid scanner didn't recognise URL QRs (had to use phone camera), each moment was named plainly with no blame. Number One's job each time: name the cause, ship the fix, log for next time.

**"Even keel, lets get to the end and then assess :)"** — after Batch B was working, the natural pull was to stop and tidy. Captain elected to press on. We landed Batch C the same watch. The right call given momentum.

**"My finger print ;) ;)"** — gentle correction when Number One wrote "Face ID / fingerprint" generically instead of crediting the actual biometric used. Pixel 8 Pro has both; Captain chose fingerprint for the bootstrap.

**"Will be leaving in a minute to go to my parents, will be back mid to late afternoon :)"** — bath-protocol-equivalent for an extended away. Number One held the watch over the GitHub Pages diagnostic and had a clean briefing ready on Captain's return.

---

## State at watch end

**Code:**
- Live repo committed + pushed: PROTOCOL.md §14 + §14.14, `org-login.html`, `v5-test.html` device-fingerprint helper + 2-button mobile-nav fix.
- Test repo committed + pushed through `9d7a4e2` (renormalize): all of Batch A + Batch A patch + Batch B + Batch B fixes + Login TTL bump + Batch C + renormalize.
- Test Worker deployed with all the above (Cloudflare API was unreliable but every push eventually succeeded after retries).

**GitHub Pages CDN: stuck** serving the last successful deploy (`Batch B debug patch` = pre-Batch-C). Re-run of failed deploy #108 queued; recovery path documented (re-run failed jobs OR push no-op commit OR cancel stuck workflow #106 via API).

**Captain's hardware:**
- Pixel 8 Pro v5 credential active, fingerprint-bootstrapped on `irlid.co.uk/org-login.html`.
- Bootstrap session (24h sliding TTL) issued during the afternoon's smoke test — Captain is technically still "logged in" if the test Worker's `login_sessions` row hasn't been touched.

**Next watch's first moves:**
1. Confirm Pages CDN is unstuck (visit OrgCheckin.html?dev=0&v=batchc-live). If not, force-cancel stuck workflow or push no-op.
2. Once Pages serves Batch C: complete the smoke test — sign in via QR, see create-org form, create first org, dashboard loads.
3. Restore §14.10 generic auth_failed via env-flag-gated debug mode (Option 2 from afternoon menu).
4. Plan Batch C.5 (staff scan-in) and Batch D (website scrape).

**Outstanding from this watch into next:**

- HANDOVER-Batch6 bake-off (Mr. Data Tuesday — that's tomorrow). v5 identity-resolution server-side fix for "Unknown" cross-device viewer placeholder. Real bake-off candidate.
- Talon surface scan report still pending (>24h since submitted).
- The IRLid app's QR scanner doesn't recognise URL QRs (plain `https://...` payloads) — Captain had to use the phone's native camera to open the login QR. Three options flagged in chat: leave it (camera handles URLs natively), extend IRLid scanner to detect URL-shaped QRs, or give login QRs a custom prefix `IRLOGIN:...`. Defer to Captain's call.
- Test-env DB orphan cleanup (Batch A.5) — wrangler API was too flaky to run inventory queries reliably today.
- cym13 r/netsec follow-up post hasn't gained traction (3+ days). Defer / let it run its course.

---

## Token usage

Long stretch but well-paced. Pro plan absorbed it. Several Cloudflare API timeouts cost retries but no wrangler operation ultimately failed; just slow.

— Number One, watch continuing into afternoon, 4 May 2026
