# Session 2026-07-02-01 — air-tight watch: audit → v6.4.20 → promotion pivot

## What was done

1. **Full local + online audit** (Captain: "find any flaws"). Result: ship healthy — live=local
   (v6.4.19→now v6.4.20), all 13 consumer pages 200, both Workers alive, 110/110 tests, deploys green.
   Flaws found: offline-queue silent drop (known, still open), 10 days of uncommitted DREAMS/PROTOCOL
   work, `.claude/` not gitignored, 2 stale open PRs.
2. **`547d224`** — landed parked DREAMS entries (28 Jun–2 Jul) + PROTOCOL §10.3 lidar-geometry anchor
   rows; `.claude/` gitignored.
3. **`ac74c30` = v6.4.20 LIVE** — offline-queue fix. Signed-action endpoints (create-and-bind,
   bind-additional-key, rebind, PATCH /org/expected/:id) excluded from QUEUE_ELIGIBLE_PATHS (nonce/
   freshness envelopes can never replay → fail loudly at click time). quarantine() dispatches
   `irlid:queue-dropped`; Org.html toasts the user. `isQueueEligible` exposed for diagnostics.
   **Rigorous localhost verification:** 18/18 eligibility cases via preview_eval on the running page;
   full behavioural chain (enqueue → replay → 404 → quarantine → event → red toast → queue drained)
   proven against the live Worker with a harmless 404 probe; console clean (only pre-existing
   geolocation denials); node --check ×3; 110/110 tests. Live serving verified post-deploy
   (QUEUE_EXCLUDED_PATTERNS present in https://irlid.co.uk/js/orgapi.js, SW v170).
4. **PRs #126 + #84 closed** with comments via GitHub API (git credential manager token, never echoed).
   #84's closeEventModal() already on main ~L8022. Verified v6.1.20-auto-checkout branch content is
   reimplemented on main (maybeAutoCheckoutAfterEventGrace) before judging it safe.
5. **Branch sweep verified but NOT executed** — permission classifier blocked mass delete (11 branches
   beyond named scope). All 11 codex/* verified merged/superseded; one-liner handed to Captain.
   `recovered/assistqr-protocol` kept.
6. **Community survey + Promotion Round 3 strategy** — appended to PROMOTION.md. Key finds: Zoom×World
   (Apr 2026), deepfakes ~11% of fraud, Encointer criticism = IRLid differentiation, EAI SecureComm
   late track deadline "10th July" (year typo'd on page — email to confirm), grant-compliance vertical
   (funders need PROOF of volunteer hours; incumbents record assertions, we mint verifiable receipts).

## Techniques worth keeping

- **Extracting the git credential for API calls:** `git credential fill` fed via `cmd /c "... < file"`
  stdin redirect (plain PowerShell pipe returned nothing; bare invocation with no stdin HANGS — 2min
  timeout). Mask the password line before printing anything.
- **preview_eval on the localhost rig can drive IndexedDB + CustomEvents end-to-end** — the whole
  offline-queue behavioural test ran without hardware and without touching production data (404 probe
  against a nonexistent Worker route = terminal-4xx simulation with zero mutation).
- **preview_screenshot can time out on Org.html** (heavy renderer) while preview_eval stays responsive —
  don't fight it; eval-based assertions are stronger evidence anyway.

## Blocked / waiting

- Branch sweep one-liner → Captain's hand.
- SecureComm late-track deadline confirmation → Captain's email (organisers).
- Imbue demo still unhappened; promotion pivot is the new track.

## Next session's first move

If Captain green-lights the spearhead: draft Show HN post + SecureComm 4-pager skeleton + grant-vertical
one-pager (in that order). Also still open: receipts-history view (now promo-relevant), Records & ID
inert inputs, Becky stale row, Discussions welcome post.
