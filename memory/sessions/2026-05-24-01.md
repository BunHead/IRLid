# Session log — Sunday 24 May 2026 afternoon (port-day arc)

**Number One:** commissioned this watch from the inheritance letter pushed Saturday afternoon (`successor-2026-05-24-afternoon.md`). Captain's energy: Sunday-afternoon-fresh, bath + exercise behind him, port-day mood explicit. Watch opened ~early afternoon BST.

## Watch in three movements

**Movement 1 — Inheritance + the bash-mount trap I almost walked into.** Confirmed HEAD at `7af9af1` matching the Saturday letter, then bashed `git status` from sandbox and got a *50-file mass-modification* result that turned out to be the OneDrive-mount-stale-cache lying exactly as `CLAUDE.md` warned. I almost reported the phantom as real ("CRLF flip, recommend `core.autocrlf=true` + renormalisation") before catching that DREAMS.md showed "truncated mid-line" via bash *and* "intact at line 1104" via native Read. PowerShell `git status` from Captain's machine confirmed: working tree clean, only DREAMS.md modified (the previous Number One's 2026-05-24 entry on Sybil / 23% bug / Counsellor Troi) plus the untracked successor letter. Real lesson banked for the watch: state-mutation questions belong to PowerShell, not bash. Bash is fine for reads-from-git-network operations (`git fetch`, `git ls-remote`); anything that reads the working tree gets the OneDrive cache lie.

**Movement 2 — T.I.N Man inscribed.** Captain agreed to fill the long-standing T.I.N Man gap during the wait. He shared `D:\SkyDrive\Pen Drive\T.I.N Man\Documents\RD13 May 2014.docx` content directly via paste — the May 2014 University of Derby Postgraduate Research Framework submission *An Investigation into Large Scale Optical Fibre Networking and the Final Mile Connection Solution* under Professor Nicklaus Antonopoulos with Dr Richard Hill and David Vorhis acknowledged. T.I.N = Total Information Network, named also after the TNG episode (S3E20) — Tam Elbrun and Gomtuu finding each other across institutional misfit. The doctoral argument for a UK-wide, publicly-owned optical fibre network covering every home including the final mile, framed against UN Article A/HRC/20/L.13 (2012) affirming internet access as a human right. The browser simulator at `TinMan-Backup.html` (Google Maps API, Derby-centred, A* with fuzzy logic, road/elevation/population biased pathfinding) is the engineering proof; the document is the policy case. **T.I.N Man IS the PhD** — not a side project that fed into it. The technical problem got mooted by Starlink launching in 2015; the public-ownership argument and the missing-infrastructure shape carried forward into every subsequent Captain project. Captain's "I've never been able to find a sponsor, always been terrible at selling myself :s" deserved a warm push-back: the work is patient and supervised; T.I.N Man arrived a year before Starlink, the better part of a decade before BT Openreach started serious full-fibre. Repeated near-miss is *timing*, not *selling*. Multiple previous Number Ones have made the same observation independently. Inscribed in two places: new section in `CLAUDE.md` ("T.I.N Man — the load-bearing precursor (2014)") between *Who He Is* and *How to Work With Spencer*; new file `memory/observations-across-watches.md` with the cross-watch through-line read as its first entry. Committed `bab7487` on origin/main.

**Movement 3 — PR-A landed clean; PR-B in flight.** Fired Mr. Data's PR-A Codex prompt mid-afternoon after Captain TLDR'd the HANDOVER via yes/no questions (four decisions locked: pacing pause between PRs / strip-and-clean for PR-C / Records & ID visible with hint banner / `websiteScrapeBtn` retired in PR-D). Mr. Data delivered `codex/v5.11.0-port-A-schema` at commit `a2a864b` in ~10 minutes — single file `apply_v5_11_0_calendar.ps1` (+117 lines), defensive three-branch logic on legacy `org_expected` exactly as designed. Bash-diff confirmed scope clean: zero other files touched. **Surfaced finding worth the bash-diff:** production `org_expected` in main has a legacy shape (`id INTEGER AUTOINCREMENT`, `org_code`, `first_name` + `surname`, `device_key_fp`, `prototype_role`) incompatible with the v5.11 spec (`id TEXT`, `org_id`, `display_name`, `device_pub_fp`, `role_key`, `archived_at`). Mr. Data's defensive branch handles this safely (`PRAGMA table_info` → if legacy shape, leave untouched, print yellow warning) but PR-B has a real design decision waiting. Captain merged PR-A as PR #37 (merge commit `f3dd95f`) while I was typing the T.I.N Man inscriptions push — the second `git push` got rejected with "fetch first" because origin had moved. Standard race; resolved with `git pull`.

## PR-A migration ran clean

Captain ran `.\apply_v5_11_0_calendar.ps1` against `irlid-db-org` remote. Output traced exactly: nine `wrangler d1 execute` blocks (3 for rooms incl. indexes / yellow warning for legacy `org_expected` / 3 for weekly_events / 3 for event_expected), all sub-millisecond (0.25-0.86ms), green ending *"v5.11.0 calendar schema migration complete. Safe to re-run; no existing tables are mutated."* The defensive branch fired exactly where it should have. Three new tables (`rooms`, `weekly_events`, `event_expected`) now exist in v5.11 shape with their indexes; legacy `org_expected` preserved per PR-A no-mutations rule.

## PR-B design decision: data loss accepted

Captain's call on the legacy `org_expected` schema collision: **accept data loss** (option c of three — vs. rename or column-mapping migration). Reasoning: no production users on the legacy data; "expected to be setting a new Org up from scratch." Cleanest path. Resolution mechanic: **PR-B includes a separate reset migration `apply_v5_11_0_org_expected_reset.ps1` that drops the legacy and recreates with v5.11 shape**, deployed atomically with the Worker code change in a 4-step sequence (merge → git pull → run reset → wrangler deploy). Brief broken-state window between reset and Worker deploy (seconds) — any inflight legacy doorman traffic 500s briefly then resumes against new shape. Acceptable for the Sunday deploy.

`CALENDAR-SPEC.md §10.3` PR-A bullet rewritten to reflect actual merged behaviour (defensive branch on legacy `org_expected` fired clean) and PR-B bullet now describes the new reset migration + deploy sequence. `HANDOVER-CalendarLivePort-v5.11.0.md §4 PR-B` gained task B.0 for the reset migration, time bumped 60-90 → 70-100 min, file list updated. §6 *Notes for Captain* gained the mandatory 4-step deploy sequence. Committed `9d58476` on origin/main.

## PR-B fired

Mr. Data's PR-B Codex prompt drafted from the updated HANDOVER; paste-ready in this session log close. Captain fired it ~mid-afternoon. Mr. Data started fresh from `origin/main`, read the updated handover/spec and the merged PR-A migration, confirmed scope: *"I'll start PR-B from fresh origin/main, read the updated handover/spec and the merged PR-A migration, then make only the reset migration and Worker changes. This one has more moving parts, so I'll verify the auth and CSV behaviour as far as the local Worker setup allows before attempting the PR."* Working at the watch-close-of-this-log timestamp.

## State at watch midpoint

- `origin/main` at `9d58476` (5 commits ahead of inheritance arrival point `7af9af1`).
- D1 `irlid-db-org` has v5.11 calendar tables (rooms, weekly_events, event_expected); legacy `org_expected` preserved awaiting PR-B reset.
- Mr. Data working PR-B (codex/v5.11.0-port-B-worker). 70-100 min estimated.
- Pending: bash-diff PR-B when delivered → Captain merge → run reset migration → wrangler deploy → bash-verify endpoints respond → plan PR-C.

## Codex prompt for PR-B (banked for reference)

```
PR-B of the v5.11.0 live port to irlid.co.uk/Org.html — Worker endpoints + legacy org_expected reset.

Read first:
- HANDOVER-CalendarLivePort-v5.11.0.md (especially §2 out-of-scope, §4 PR-B tasks B.0 through B.8, §6 Notes for Captain for the deploy sequence)
- CALENDAR-SPEC.md (§1 schemas, §2 endpoints, §4 CSV format, §10 migration path)
- The merged PR-A migration apply_v5_11_0_calendar.ps1 for pattern reference

Scope (PR-B only):
- NEW file: irlid-api-org/migrations/apply_v5_11_0_org_expected_reset.ps1 per HANDOVER §4 PR-B B.0. DROP TABLE IF EXISTS org_expected then CREATE TABLE in v5.11 shape per CALENDAR-SPEC §1 plus three indexes. Print yellow notice on entry.
- MODIFY: irlid-api-org/src/index.js — 11 calendar endpoints per CALENDAR-SPEC §2 plus HANDOVER §4 PR-B B.1's endpoint table
- Use existing helpers; CORS unchanged; cross-org Expected ID injection guard; CSV vocab-flex + auto-create; ?replace=1 query param

Branch: codex/v5.11.0-port-B-worker
PAUSE after this PR. Deploy sequence: merge → git pull → reset migration → wrangler deploy.
```

## Pacing-pause discipline holding

Captain explicitly chose pause-between-PRs over chain-and-review-at-end. PR-A → merge → verify → PR-B fire → (now) wait for delivery → bash-diff → merge → reset migration → wrangler deploy → bash-verify → only then PR-C. Each PR independently revert-able; `OrgCheckin.html` is the rollback URL the entire time.

## Open carry-forwards (parked for after port lands)

- Cloudflare token rotation (`cfat_wIMFM4RI…` + `cfut_YZ11ouJO…`) — 8 days exposed now.
- `codex/v5.10.1-path-b` branch deletion on origin (+ 8 other stale `codex/*` branches).
- PROTOCOL-Records-Broker.md promotion to `PROTOCOL.md §X`.
- Bug E (bio-metric=0 in doorman flow) — architectural call.
- Promotion-round-2 drafting (Patreon post-v5.11.0-green; Wisdom humanitarian re-engagement; landing one specific shot rather than throwing rope per Captain's "yet another stab" fatigue framing in the inheritance letter).
