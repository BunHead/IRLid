# Session — Saturday 23 May 2026 afternoon (B2 live + Port HANDOVER drafted + Org.html ratified)

**Number One on watch:** Saturday afternoon. Captain returned from morning R&R around ~16:00 BST and worked through the close of the watch.

## The arc

Watch opened with Mr. Data's tokens recovered, B2 brief ready to fire, CALENDAR-SPEC.md and orphan-button sweep queued for Number One to work on. Closed with **B2 live in production**, **v5.11 port HANDOVER drafted and pushed**, and a **brand new architectural ratification from Captain**: the port lands at `irlid.co.uk/Org.html` (replacing the existing 1.9KB redirect shim), not `OrgCheckin.html` — a six-year-old typo magnet finally retired.

The watch surfaced two architectural confusions worth re-stating clearly (Number One initial framing wasn't sharp enough):

1. **Org.html is the full portal**, behaving exactly like OrgCheckin.html does today — single page that gates on session state (QR sign-in screen if no session, dashboard if session). `org-login.html` is the phone-side scan target, unchanged.
2. **Worker / D1 keep `-org` suffix.** The suffix isn't a typo magnet — it disambiguates the org portal Worker from the public-facing `irlid-api` Worker (and same for D1: `irlid-db` is public, `irlid-db-org` is portal). Only the HTML filename was painful.

## What landed (in order)

### Live (origin/main)

- **`02ee13e`** — CALENDAR-SPEC §1+§8+§10 updated for Org.html port target + HANDOVER-CalendarLivePort-v5.11.0.md drafted (Captain pushed end-of-watch).
- **`0270a63`** — Merge of PR #36 (B2: attendance window means today). Closes Bugs A (yesterday-as-today), B (duplicate IN+linked-expected rows), D (phantom CHECKED OUT). Mr. Data PR clean (~30 lines Worker + ~20 lines client).
- **`ebb0236`** — Combined push: CALENDAR-SPEC v2 rewrite + T4.3.61 (Export/Import button pairing) + orphan-button sweep report.
- **Worker `irlid-api-org` deployed at version `9ad0442e-1042-4366-be5b-059e69c8e169`** — B2 fix now live in production.
- **Branch `codex/v5.11.2-attendance-window` deleted from origin** (post-merge cleanup).

### CALENDAR-SPEC.md v2

Full rewrite of the 19 May draft. Replaces a date-based events table + RRULE-lite recurrence assumption with the weekly-cycle model Captain ratified in the morning bath-watch. New four-table data model: `rooms` + `org_expected` (Option C stable IDs) + `weekly_events` (day_of_week 1-7) + `event_expected` (link table). Section 0 explicitly ratifies the ten architectural decisions made across the day so they're not re-litigated inside the port. Section 4 documents the CSV vocabulary-flex column headers. Section 10 fully restructured into starting state / target state / four-PR sequence / rollback.

Three afternoon edits added the Org.html context: §1 intro states the rename ratification and confirms backend stays the same; §8 acceptance criterion #3 changed from "live OrgCheckin.html" to "new Org.html" with explicit guard that OrgCheckin.html is NOT modified; §10 expanded into §10.1-§10.4 sub-sections.

### Orphan-button sweep (memory/orphan-button-sweep-2026-05-23.md)

Walked all 75 button-IDs in `OrgCheckinTest.html` + 240 ID-less data-* delegated buttons. Findings:

- **74 of 75 wired**; 1 intentional disabled placeholder (`websiteScrapeBtn` with "coming soon" copy).
- **240 delegated buttons** all verified via class/data-* listener patterns.
- **Live vs mockup parity:**
  - 3 buttons in live only: `csvBtn` + `csvUploadBtn` (intentionally retired in favour of per-event CSV), `signInHereBtn` (parity gap — should port back).
  - 22 mockup-only `v511-*` buttons + `inviteStaffBtn` (relocated) — all have Worker endpoint targets defined in CALENDAR-SPEC §2.
  - 53 buttons in both, no port change needed.
- **`inviteStaffBtn` Captain flag explained:** opens dialog correctly via existing `openInviteStaffDialog`, but the dialog's create-invite step calls a Worker endpoint that has no session in the mockup. Live-backed orphan — works the moment a Lead Admin signs in.

Mockup is **port-ready** as of T4.3.61.

### T4.3.61 (mockup polish)

Captain spotted Export week + Import week buttons sitting too far apart in the calendar toolbar (the parent flex container has `justify-content: space-between`). Quick fix: wrapped them in a new `.v511-week-csv-pair` inline-flex sub-group with 4px tight gap. Now they pair visually while the toolbar's distribution stays clean.

### HANDOVER-CalendarLivePort-v5.11.0.md (~10KB)

Substantial multi-PR brief targeting Org.html (replaces redirect shim). Backend names stay `irlid-api-org` + `irlid-db-org`. OrgCheckin.html NOT modified.

Four-PR stack:
- **PR-A** D1 schema — idempotent migration creating 4 new tables + indexes. ~30-45min.
- **PR-B** Worker endpoints — all 11 CALENDAR-SPEC §2 endpoints. `requireDevOrStaffSession` minimum, `requireSignedAction` on manager+ writes. ~60-90min.
- **PR-C** Org.html UI — built from `OrgCheckinTest.html` with mockup-only tooling stripped (prototype-role simulator out, localStorage state replaced with Worker calls, seed data removed). Wires every button to its endpoint per the orphan sweep map. Ports back `signInHereBtn` for same-device WebAuthn sign-in. Build pill `v5.11.0` on Org.html only. ~90-120min.
- **PR-D** Cutover — README + roadmap URL updates, optional retire of `websiteScrapeBtn` placeholder. ~20-30min.

Six explicit out-of-scope guards in §2: don't touch OrgCheckin.html, OrgCheckinTest.html, js/, public `irlid-api` Worker, `org-entry/login/scan` pages, existing D1 tables.

Mandatory PR order. Mr. Data instructed to pause between each PR for Captain review.

## Notes banked

- **Org.html was already booked.** A 1.9KB redirect shim has lived at that URL since 10 May (set up during the v5.9 live port to catch bookmarks). The shim has been waiting for the real file to land. Captain forgot it was there — the architectural call "make the new site Org.html" was actually returning to a name that was always meant to be the future.
- **"It was a fiddle a long time ago, been a pain spelling it ever since."** Captain's framing of why the rename matters. The `OrgCheckin` name was a workaround that calcified into an obstacle. Six years of mistyping it; six years of dictation friction; six years of bookmark editing. Returns to clean now.
- **Mr. Data couldn't open the PR from Codex** (GitHub connector returned 403). The branch was pushed clean; Captain clicked the compare URL to open the PR manually. Worth flagging for future briefs — Mr. Data should always include the compare URL in his close-out report when PR creation fails.
- **B2 deploy proceeded cleanly.** No wrangler API timeout this time (the four-receipt pattern in BOOTSTRAP §6 didn't fire). Worker version `9ad0442e` live in production.
- **Captain's order-worry was unfounded.** He ran (pull + push spec/HANDOVER) → wrangler deploy → branch delete in that order; worried it should have been (wrangler first). But the spec/HANDOVER push is markdown only — doesn't affect production. Order was fine.

## What's queued for Captain's return / Sunday morning

The port HANDOVER is **drafted and pushed**, **not fired**. Captain explicitly chose to sleep on it rather than fire at end-of-Saturday. Reasoning: (1) smoke B2 in production first to confirm clean baseline before building on top, (2) read the HANDOVER before firing rather than firing blind, (3) energy management — substantial workstream shouldn't open at natural close of a long watch.

When Captain returns:
1. **Hard-refresh and smoke B2 in production** (if not done end-of-Saturday). 5-step checklist in HANDOVER §6 + the morning session log.
2. **Read HANDOVER-CalendarLivePort-v5.11.0.md** — focus on §2 (out-of-scope) and §4 (PR stack).
3. **Decide to fire (or revise first)**. Codex prompt is short — read brief, work PR-A first, pause between PRs.
4. **PR-A bash-diff** waits for Number One when it lands.

## What's NOT done (deferred / parked)

- ~~**B2 production smoke**~~ — **DONE end-of-watch.** Captain ran the 5-step check on `irlid.co.uk/OrgCheckin.html` with 8Pro + 4a out-then-in cycle. All five points green: build pill v5.11.2, no yesterday rows in Today, no duplicate rows, CHECKED OUT count honest (matches the cycle's real checkouts), fresh re-check-in works (scan_count 1→2, last_seen updated, status IN). Bonus: "first seen" timestamps preserved across the cycle — historical data untouched. Bugs A/B/D officially closed in production.
- **Cloudflare token rotation** — outstanding since 17 May (now seven days).
- **`codex/v5.10.1-path-b` branch deletion** — outstanding since 17 May.
- **Other stale `codex/*` branches** (eight more on origin) — separate housekeeping pass.
- **PROTOCOL-Records-Broker.md** promotion to PROTOCOL.md §X.
- **Bug E** (bio-metric=0) — architectural call, parked.
- **Records & ID wiring** — explicit "ships mockup-only this port; v5.13+" decision in HANDOVER §2.
- **`websiteScrapeBtn` vs `themeScrapeBtn` consolidation** — flagged in orphan sweep §6.3 as an open question for Captain.

— Number One, watch closes with port path locked, B2 live, and the cleaner URL one merge-day away.
