# State of Play — IRLid

**Last refreshed:** Wednesday 20 May 2026 evening — **`v5.11` mockup advanced through T4.3.29 → T4.3.36: eight patches across a morning + lunchtime + evening watch. Records & ID condensed via expanders; Stream particle system born + reborn as JS-injected randomised DOM particles with full physics (count/spread/offset/mirror/gravity/colour-source sliders); QR dissolve gained dir9 directional burn with jittered burn-line; anchor crosshair visualiser added (mix-blend-mode auto-contrasts on any backdrop); fullscreenchange refresh wired.** **T4.3.29** Records & ID tab folded into 4 closed-by-default expanders (Storage destination / What to capture / Privacy & retention / Why this matters), banner + status row stay visible at top. **T4.3.30** new Breath particle effect (initial radial-gradient version) + Flame's image-anchor now honours horizontal/vertical/quad symmetry across all gradient-stack configurations so multi-dragon scenes get fire from every position. **T4.3.31** Breath rebuilt from scratch as JS-injected randomised DOM particles (Captain's screenshot showed the radial-gradient version was wrong — wanted real particles like confetti but cone-shaped, randomised, anchored to origin); dir9 directional control; symmetry-aware multi-anchor emission. **T4.3.32** Breath → Stream rename ("don't think most people would know what breath was"); slider setting type added to schema renderer; particle-count + stream-width sliders; mirror-on-symmetry toggle so right dragon blows LEFT when left dragon blows RIGHT under horizontal symmetry; per-row z-order toggle (▲/▼) lets any effect render above OR below the QR+image with `cel-z-below` class dropping fx-layer + stream particles to z-index 2. **T4.3.33** Stream anchor offset sliders (X/Y, ±40%, axisFlip-aware so symmetric mirroring keeps per-side nudge correct — left dragon's mouth-offset becomes right dragon's mirrored mouth-offset automatically); new QR dissolve variant with dir9 directional burn (linear-gradient mask + radial for centre `c`). **T4.3.34** per-mode Play-sound on/off toggle (`v511PaPlaySound` added to PER_MODE_FIELD_IDS, gates `playOutcomeTone` at entry, live checkbox wins over stored state); anchor crosshairs visualiser for Stream (yellow plus-mark, pulses, updates on selection / slider input / chip click / symmetry change); dissolve mask 3-layer composite with `mask-composite: add` and ±12° angle jitter for ragged burn-line vs laser-clean. **T4.3.35** crosshair contrast via `mix-blend-mode: difference` + white ink (auto-inverts against any backdrop — black on white, white on black, complementary on coloured); gravity toggle on Stream (JS adds downward dy-bias per-particle, ~45% of stage longest dimension, NOT mirrored by axisFlip since gravity always pulls down) + Confetti (CSS animation-timing-function swap to cubic-bezier ease-in for acceleration feel); colour-source chip (`fire | cycle palette`) wired on Stream + Flame so the celebration palette swatches drive particle colours; palette-driven gradients added for all five Flame symmetric configurations. **T4.3.36** fullscreenchange document listener — re-runs `applyImagePositioning()`, `renderPreviewQr()`, `v511UpdateAnchorCrosshairs()` after a 2-rAF delay so layout settles. The structural caveat that surfaced: normal-view stage is clamped by `max-height: 48vh` and its actual aspect ratio diverges from `--preview-aspect` (which is set from `window.screen`); fullscreen takes screen aspect exactly; image height-as-%-of-stage shifts between modes because `background-size: 15% auto` pins width but lets height ride natural image aspect. Anchor offset that lands on dragon's mouth in one mode may need re-tuning for the other; banked as "per-mode anchor offsets" next-step idea if it bites in practice. **Build pill at watch close:** `v5.10.2 + v5.11 mockup T4.3.36`. **Captain's verdict mid-watch:** *"Excellent work Number One"* (afternoon, before work) and *"will see you after work ;) ;)"* with an Example.mp4 file (the dragon scene demo, presumably). Watch closed at Number One's retirement with all eight patches on origin/main and full memory writeup. **Open carryforward:** intermittent Background animation bug still needs repro; per-mode anchor offsets banked as next-step idea; Mr. Data port of mockup → live `OrgCheckin.html` still the genuinely-next architectural move; calendar implementation + PROTOCOL §X-Records-Broker ratification + Cloudflare token rotation + `codex/v5.10.1-path-b` branch cleanup still pending.

---

**Earlier:** Tuesday 19 May 2026 evening — **`v5.11` mockup celebration animation SHIPPED end-to-end: T4.3.3 → T4.3.28 — 26 surgical patches, every effect setting now visually wired, save round-trip complete.** Long Tuesday evening watch following the morning's library + sequence + settings-pane rebuild. Captain explicitly asked for surgical-patch discipline ("smaller parts, so you're not redoing the entire page each time"); the rhythm held across all 26 bumps — each touched one isolated concern, build pill bumped per ship, Captain tested before next began. **Headline progression:** **T4.3.3-5** background cycle direction grid save round-trip (capture/restore via `aria-pressed` + radial state; chip restore extended to reapply `bg-bounce-*`/`bg-trans-*` stage classes); **T4.3.6** custom welcome message → text overlay piping (removed four hardcoded `::before { content: '...' }` rules; live `v511PaWelcome` value piped through `v511ApplyItemSettings` with template chips wrapping it `welcome` verbatim / `name` → "..., Kerry!" / `name + action` → "Kerry — checked in"); **T4.3.7** stable-ID rebuild fix for settings preservation (`v511ReadSequenceFromDom` was rebuilding sequence items fresh on every 300ms debounced save and silently DROPPING `item.settings` — every chip selection vanished after typing in any field; fix: assigned each sequence row a stable `data-seq-id`, matched it back via `byId` map to preserve `item.settings`); **T4.3.8** outer-glow keyframe rebalance for visibility (20%-80% hold instead of brief 50% peak); **T4.3.9** shake axis variants (vertical / both); **T4.3.10** outer-glow intensity (3 variants louder than v3); **T4.3.11** glow halo sweep + sweep keyframes (inward / rotate); **T4.3.12** pulse intensity (3 ring radii: 10/22/40px); **T4.3.13** density variants for confetti/sparkles/particles; **T4.3.14** Deny default sequence trimmed from `[Shake, Delay, QR-disappear]` → `[QR-disappear]` (auto-shake removed per Captain); **T4.3.15** real QR dissolve via `mask-image: repeating-linear-gradient` (stripe-based pixel dissolve replacing scaleX/Y misnomer); **T4.3.16** iris wipe direction variants (3 new keyframes for close-then-open / open-then-close / close only / open only); **T4.3.17** glitch kind variants (hue / displace separation); **T4.3.18** glow centre pulse on/off (halo-only mode); **T4.3.19** ripple thickness (border-width 2/9/16px); **T4.3.20** **Count loop in `v511PlaySequence`** — cam-flash / strobe / ripple / bg now fire N times within item duration via inner setTimeout loop with double-rAF gap between sub-fires; **T4.3.21** **click-in-fullscreen plays Sample** (dblclick-debounced single-click handler that only fires when `document.fullscreenElement === sampleStage`); **T4.3.22** **"Play with previous" overlap toggle** on every sequence row (small `+` button between duration and remove; pressed = step N+1 fires concurrently with step N; CSS `:first-child` hides on row 0; `withPrev` field round-trips via `v511ReadSequenceFromDom`; playback architecture extended with `chainNextNow()` helper that bypasses cleanup setTimeout when next step is overlapping); **T4.3.23** scale-punch zoom direction (in / out) + intensity (6 keyframes total); **T4.3.24-25** QR dissolve hold-state genuinely gone (opacity ramps to 0 at peak, gone-window widened to 35-80% of cycle, mask tightened at peak); **T4.3.26** **WAV save round-trip + Reset button + mode-aware filename label** — bug discovered: `uploadedWavs[mode]` + `uploadedWavNames[mode]` were globals never captured by `v511CaptureState`; fixed by adding to save state + restoring in `v511LoadState` step 6.9 + firing `v511SaveDebounced()` on upload + new Reset button clearing the WAV per active mode + tiered quota fallback (strip imageData first → WAVs next → minimal save); **T4.3.27** **bounce vs loop wiring** — `cel-bounce-eff-loop` class now triggers `animation-iteration-count: 2` + `animation-duration: calc(var(--cel-cycle-dur) / 2)` so two complete cycles fit in same step duration; wired across outer-glow / spotlight / pulse / qr / scale-punch; **T4.3.28** **whole celebration sweep complete in one drop** — pulse colour source (cycle palette walks pal-1/2/3) + spotlight intensity + qr-disappear easing (linear/snap timing-function swaps) + qr-disappear intensity + flame intensity (3 variants) + sparkles/particles intensity + confetti completes 9-direction grid (added tl/tr/c) + confetti single colour source + sparkles full 9-direction grid (added l/r/tl/tr/bl/br drift variants) + particles full 9-direction grid (8 directional translate-while-scale keyframes) + ripple cycle-palette colour source (animated border-color through palette).

**Captain's verdict mid-watch:** *"Chimp brain definitely approves ;) ;)"* on the QR dissolve peak; the dragon-themed test setup ("Welcome Everyone to this Happy Place!" + symmetric Hawthorne Devon Grey-primed dragons + blue palette) demonstrated the full theme customisation stack functioning end-to-end as a real configurable scene.

**Status at watch end:** All celebration animation sweep items closed; Captain called the dock. Build pill at watch close: **`v5.10.2 + v5.11 mockup T4.3.28`**. One bug remains open (intermittent — needs repro): Background animation doesn't always play; will chase when Captain can reproduce.

---

**Earlier same day:** Tuesday morning watch — long stretch T4.1.10 → T4.3.2. Bath-watch landed three spec drafts (`SETTINGS-REVAMP-SPEC.md`, `CALENDAR-SPEC.md`, `PROTOCOL-Records-Broker.md`). Visual theming tab restructured deeply: Pattern + Image overlays nested INSIDE Background body; QR customization nested INSIDE Post-Accept body via JS reparenting; celebration palette per-mode with mode-tinted defaults (Allow greens / Review ambers / Deny reds); biggest re-architecture since Tier 1 — celebration animation rebuilt as library + sequence two-pane builder with 20 effects + Delay timing + per-item duration + per-effect settings schema. Captain reported "very very good" structurally but called for rest with gaps captured. Those gaps fed directly into the evening watch's 26-patch sweep.

---

**Earlier same morning:** Three spec drafts landed in live repo root (Tuesday bath-watch): `SETTINGS-REVAMP-SPEC.md` (11 sections, Mr. Data implementation brief for porting v5.11 mockup → live OrgCheckin.html with 4-phase rollout + schema), `CALENDAR-SPEC.md` (10 sections, multi-room + per-event Expected list + Swim-lane + CSV pipeline, `events` + `event_expected` D1 schema + 9 new Worker endpoints), `PROTOCOL-Records-Broker.md` (candidate PROTOCOL.md §X, broker-not-store architectural commitment with `org_storage_connectors` + `records_references` schemas + 7 Worker endpoints + threat model + re-enrolment pattern). All committed + pushed in `86ad073`.

---

**Earlier:** Monday 18 May 2026 midday — **`v5.11` Visual theming tab deeply wired in `OrgCheckinTest.html` sandbox.** Single long-watch stretch from ~08:00 through midday: visuals-functionality wired tier-by-tier (Tier 1 live wiring → Tier 2 per-mode state + image upload → Tier 2.5 selectable palette colours + expandable effects + drag-to-layer → light mode comprehensive support → Tier 2.6 palette remove + real QR.png placeholder + restored Image settings UI → Tier 3 QRCode.js-generated real QR + image positioning + alpha cycle + layer numbers → Tier 3.5 aspect-match preview + position grid sizing + QR render fix + WAV upload → Tier 3.6 Off-mode-palette + anchor offset bump + image scale slider + celebration palette flowing into ALL effects + symmetry layouts + QR motion variants). Visual theming is now genuinely interactive end-to-end: light/dark toggle, palette swatches drive page-cycle + Glow/Pulse/Pattern/Text colours, Image upload + position grid + scale + symmetry + alpha cycle all functional, 7 QR motion animation variants selectable, Replace button uploads custom WAV per outcome mode, Sample button fires layered celebration with audio. Captain confirmed during the watch: **visuals testing needs no Worker involvement — QR rendering is client-side; only persistence (save/load) and real check-in flow would touch the Worker.** Architectural clarity banked. **Earlier same watch (~08:00):** verified Sunday's pushes landed (origin/main at `6dd659c`), uncommitted DREAMS.md from overnight Number One session committed by Captain, Cloudflare token rotation deferred per Captain ("nothing worth stealing for a week"). **Earlier (Sunday 17 May very-late):** `v5.10.7` LIVE; v5.11 mockup structurally complete (7 tabs / expanders / drill-downs / branded masthead / now-line / etc.). Settings revamp mockup at close: 7 tabs (Organisation / Event & calendar / Roles & staff / Visual theming / Sign-in & auth / Tools & diagnostics / Records & ID), each with emoji icons + ARIA tab pattern; expander pattern applied to 5 of 7 tabs (Organisation has Brand polish & contact info expander, Event & calendar has Default event settings expander, Roles & staff has Role vocabulary + Rooms / spaces expanders, Visual theming has Advanced theming expander with Palettes/Animation sub-tabs + Post-accept behaviour expander, Tools & diagnostics has Outcome sounds + Debug + Developer expanders, Sign-in & auth has Service-account login expander); 24-hour calendar with brightness banding + per-event drill-down + multi-room with List/Swim-lane view + room filter + Day CSV export; Records & ID 7th tab carries the broker-not-store architectural commitment; Contact info display per-item opt-in (all default off); Post-accept behaviour lives in Visual theming as a visual/flow setting (not Event & calendar). **Captain's visuals deep-dive directives for next watch:** focus on check-in/out only — categories A (celebration), B (QR styling), D (calendar/schedule), E (identity/attendee), F (dashboard/live data), I (accessibility + W3C cert) in scope; C/G/H/J out of scope; K v6+ "design with maybe in mind". Inventory in `memory/letters/successor-2026-05-17-late.md`. Long single-watch stretch — global sign-out shipped end-to-end (afternoon), then Captain pivoted to Settings UX revamp using the test fork as a clickable design playground. Mockup state now: 7 tabs (Organisation / Event & calendar / Roles & staff / Visual theming / Sign-in & auth / Tools & diagnostics / Records & ID), each with emoji icons in the tab bar; 24-hour calendar with brightness-banded working-hours zone (09:00-17:59 lighter, off-hours dimmer) + auto-scroll to 09:00; per-event drill-down accordion with Expected list (sample names, status indicators) + 4 action buttons (Add person / Invite QR / Import CSV / Export CSV — same shape symmetry, the morning's "synergy" insight); top-right ↑↓ CSV buttons removed (per-event drill-down owns CSV now); multi-room model with Rooms / spaces section in Roles & Staff tab + room toolbar (List/Swim-lane view toggle + Room filter dropdown) at top of Calendar tab + inline room tags on event rows (Studio 1 blue / Studio 2 green / Studio 3 amber); 6-hour swim-lane preview demonstrating the time × room grid concept; Day CSV export at calendar bottom; expander pattern applied across 4 tabs (Roles & Staff: Role vocabulary + Rooms behind expanders, Staff list visible; Tools & diagnostics: Audit log visible, Outcome sounds + Debug + Developer behind expanders; Sign-in & auth: Service-account login legacy mode behind expander; Event & calendar: Default event settings behind expander beneath the calendar). Build pill `v5.10.2 (test)` → `v5.10.2 + v5.11 mockup`. All form clicks are no-ops by design; live save/load preserved under hidden legacy settings-shell for cycle smoke-tests. The 7th tab (Records & ID) carries Captain's "IRLid doesn't store this, we broker to your storage" architectural commitment — connects to Google Drive / OneDrive / S3 / SFTP / etc., retains hash + reference only. **Earlier same watch:** `v5.10.7` Worker + frontend ship; global sign-out is now user-visible in both directions, pretty much instantly, across 8 Pro + desktop. Long watch closing four shipped patches (`v5.10.3` CSV completeness + `v5.10.4` sign-out-two-clicks + `v5.10.5` Worker global-sign-out endpoint + `v5.10.6` same-device sign-in + `v5.10.7` Number-One-inline session-poll heartbeat). **Headline architectural win:** sign out on any device now revokes every login session for that user server-side via `DELETE /user/sign-out-all-devices` (deployed via Cloudflare dashboard Quick Edit after wrangler API timed out from Captain's home network with both User and Account tokens), AND the OTHER device notices within 30s because of the v5.10.7 session-poll heartbeat (`setInterval` calls `GET /user/orgs` with the current Bearer; on 401 fires the existing `signOutOrg()` cleanup → dashboard bounces to sign-in with the green "Signed out" toast). Both directions hardware-proven on production with `Build v5.10.7` cold from production: 8 Pro sign-out → desktop bounced; desktop sign-out → 8 Pro bounced. Captain's exact words on the second direction: *"can confirm, it work the other way around (pretty much instantly :D )"*. **Two new BOOTSTRAP §6 pitfalls promoted:** (1) after origin/main merges, `git pull` BEFORE attempting Worker deploys — the deploy slog today began with a stale-clipboard paste-of-old-code into Quick Edit because the local file on D: had never been pulled after Mr. Data's PR #27 merge; (2) wrangler / Cloudflare management API timeouts from Captain's home network — Cloudflare dashboard `dash.cloudflare.com → Workers & Pages → <worker> → Edit code → paste → Deploy` is the working fallback when wrangler's `api.cloudflare.com` calls hang. **Earlier this morning** (same watch): `v5.10.1` Path B merged at `7c7c146` (Mr. Data PR #25, Bearer-resolved authority in `requireSignedAction`); Number One inline `v5.10.2` placeholder leak fix (`9f7c220`); bootstrap-pointer chain repaired structurally (`2240368` — retroactive §10 prepend on 15 May letter + new `_TEMPLATE-successor-letter.md` scaffold). Versioning convention restored to 3-part `vX.Y.Z` + single-letter suffixes per BOOTSTRAP §4.

**Earlier (15 May 2026 afternoon):** Brief-shaped watch — three handover briefs pushed to `origin/main` at `8becca3` (`HANDOVER-PerActionAuthPathB.md` + `HANDOVER-A1-SettingsRoleGating-Refresh.md` + Friday morning section of `memory/pending-work.md`). Mr. Data eventually clean-cloned after three routing failures (Brief A1 misfire because Phase 0 brief wasn't pushed first; first attempt wired to test env; second attempt empty workspace) and pushed Path B branch awaiting Captain's review-and-merge. Three Mr. Data routing discipline rules banked: (1) trust no sub-agent for git state — verify directly; (2) every Mr. Data prompt opens with `git remote -v` verification; (3) fresh Codex sessions need explicit `git clone` instruction. Successor letter omitted §10 pointer (surfaced 16 May).

**Earlier:** Thursday 14 May 2026 late evening — **`v5.10.0.5` LIVE on irlid.co.uk; Phase 0 per-action WebAuthn HARDWARE-PROVEN end-to-end on production with multiple devices and cycle stress.** Two real-device smokes passed tonight: Kerry Austin's 4a fp bound + check-in via 8 Pro signature; Spencer Austin's check-in attempt bound + check-in via 8 Pro signature. Captain then cycled both through signed check-OUT (`OUT lock signed`) → fresh check-IN (scan_count=2). Three Worker bug fixes earlier today (v5.10.0.4 body-pass refactor + v5.10.0.5 bootstrap-fp implicit-developer-role) closed the silent-fail rabbit hole. Late evening: recovery from corrupted `BOOTSTRAP_DEVELOPER_FP` secret (single-fp clean rotation to 8 Pro's current fp `n4FzIhV_1jc2u_HO`); D1 hygiene pass (display_name update, 2 phantom portal_users + 1 stale login_session purged, 12-15 testing org_checkins rows purged); dashboard now clean — 2 attendance rows from tonight's smokes + Poppy linked expected. **Open architectural question (next watch): v5.10.1 Path B — separate signature (non-repudiation) from authority (Bearer session resolution) in `requireSignedAction` so any device with a developer-tier session can sign manager actions without needing its fp in `BOOTSTRAP_DEVELOPER_FP`.** Currently only Captain's 8 Pro (fp `n4FzIhV_1jc2u_HO`) can sign per-action bind/invite envelopes — desktop binding remains broken until Path B lands. **Earlier today** Mr. Data's stale `OrgCheckin.html` baseline clobbered the `.13.x` celebration overhaul on the v5.9.14 ship; caught + reverted within ~10 min, hand-ported additive invite work onto the clean `.13.34` baseline.

**Earlier:** Wednesday 13 May 2026 evening — **`v5.9.0.13.34` LIVE on irlid.co.uk; eight PRs shipped today, demo didn't happen (Donald no-show at imbue), three briefs queued for upcoming watches.** Briefs at repo root: **A** `HANDOVER-StaffInviteQR.md` (`v5.9.0.14` — first-class multi-device staff enrolment via one-shot signed invite QR, Lead-Admin-only issuer, replaces the multi-fp `BOOTSTRAP_DEVELOPER_FP` workaround); **A1** `HANDOVER-SettingsRoleGatingRefactor.md` (`v5.9.0.15` — open Settings to Manager-tier with per-item `data-min-role` gating, depends on A merging first); **A2** `HANDOVER-AdminActionAuditLog.md` (`v5.9.0.16` — single canonical `org_admin_audit` table + retrofit logging hooks + Settings UI surface). **Brief B (Lead Admin swap)** is fully scoped in chat but not yet written to a file: Developer-only, soft-lockdown end-of-window behaviour, old-Lead-Admin-can-block, in-app banner notifications (no email). **Spec change to formalise in PROTOCOL.md §14.9 next watch:** Lead Admin invariant tightened from "at least one, no upper bound" to **"exactly one Lead Admin per org, except during a Developer-mediated swap window"** — enforced forward by Brief A rejecting any `role=lead_admin` invite with `400 lead_admin_invite_deferred`. **Anomaly worth banking:** `.29` GIF support (branch `codex/v5.9.0.13.29-gif-support` at `6d54c97`) holds unshipped GIF code; brief lives at `HANDOVER-GifSupport.md` in repo root; salvage when GIF feature comes back up. **Operational note for future Number Ones:** the bash sandbox mount caches stale views of OneDrive-synced files — use Read/Write/Edit/Grep/Glob (Windows-native) for working-tree inspection on this repo, not bash. Captain's Windows-side Git has `core.autocrlf=true` so CRLF/LF drift is invisible from PowerShell; line-endings are healthy.

**Earlier:** Tuesday 12 May 2026 ~22:45 demo-eve final — **`v5.9.0.13.26` LIVE; Mr. Data turbo'd two queued briefs into PRs awaiting review.** PR #13 (`v5.9.0.13.27` — symmetric background image) and PR #14 (`v5.9.0.13.28` — role vocabulary carries to Viewing-as + role pills). Late-night idea stream banked in `pending-work.md` (logo wobble / GIF import / WAV on accept / particle-dissolve QR / glow trail / layer system). Captain's local main also had Number One's multi-fp Worker patch + pill bump + brief files + memory updates ready to push. Demo Wednesday 13 May still uncertain at this point.

**Purpose:** Single-glance answer to "what are we doing and why." Skim this at session start before anything else operational. Detail lives in `PROTOCOL.md`, `CLAUDE.md`, `memory/pending-work.md`.

**Authority:** This file is the canonical mapping of legacy labels (Batch A/B/C, Polish 1–11, Batch 1–16) to the new `vX.Y.Z` convention. Other files defer to it.

*(Older watch-by-watch historical entries removed in the 13 May 2026 evening prune. Recover with `git log --all -p memory/STATE-OF-PLAY.md` if needed for forensics.)*

---

## Live (irlid.co.uk)

| Item | State |
|---|---|
| Protocol version | **v5** — hardware-backed signing via WebAuthn / Passkeys |
| Consumer site live since | 2 May 2026 |
| **Org dashboard live since** | **10 May 2026 (v5.9 — Path A live port)** |
| Score with v5 features active | up to 70/100 |
| Verified surfaces | Edge + Chrome + Windows Hello on Windows 11; Chrome + Pixel 8 Pro fingerprint on Android 10 |
| Last meaningful change | **`v5.10.7`** (17 May afternoon, commit `7df1243`) — session-poll heartbeat closes the visibility gap left by `v5.10.5`: dashboard `setInterval` hits `GET /user/orgs` with current Bearer every 30s; on 401, fires `signOutOrg()` local cleanup → dashboard bounces to sign-in within 30s of another device triggering global sign-out. Build pill `v5.10.6` → `v5.10.7`; SW cache `irlid-shell-v15` → `v16`. Stacked on top of `v5.10.5` (Worker `DELETE /user/sign-out-all-devices`, deployed via Cloudflare dashboard Quick Edit because wrangler API timed out from home network) + `v5.10.6` (same-device sign-in, Mr. Data PR #28 merged earlier today). Both directions hardware-verified on production — sign out on 8 Pro → desktop bounces; sign out on desktop → 8 Pro bounces; "pretty much instantly". |
| Live Worker (Org dashboard) | `irlid-api-org` at `https://irlid-api-org.irlid-bunhead.workers.dev` |
| Live D1 (Org dashboard) | `irlid-db-org` (`484dad86-e75c-412e-9423-ca0bb27cdcb8`), region WEUR |
| Live Worker (consumer) | `irlid-api` (untouched by v5.9 port — kept production-stable) |
| First seeded org | `0337bf2f-e8a3-48d4-a12b-3f9426354f4f` "Test Event" / slug `imbue-ventures` / api_key `org_1f6acd...4256` |
| Captain's Developer membership | `BOOTSTRAP_DEVELOPER_FP = "n4FzIhV_1jc2u_HO"` (8 Pro only) as of 14 May late-evening rotation. Previous multi-fp setup (`TvklFsivZk68R67j,65u-S-W_NFxr8u1L`) was overwritten with bad content during 13/14 May's debug spiral, recovered with clean single-fp rotation. Desktop binding deferred until v5.10.1 Path B lands. |
| Phase 0 hardware proof | ✅ 14 May late evening — Kerry Austin (4a fp `Zt-xZfDmtKu5Y1sr` bind via 8 Pro signing) + Spencer Austin (Captain's check-in attempt fp bind via 8 Pro signing) + multiple check-in/out/in cycles with signed `OUT lock signed` round-trips |

---

## Test environment (bunhead.github.io/IRLid-TestEnvironment)

| Item | State |
|---|---|
| Active version | **v5.5.12** (Tier 2 offline write queue + indicator) verified end-to-end on hardware 8 May; **v5.7.1h** (audit board) verified on Huawei tablet 8 May; **v5.6** (AssistQR) in flight |
| Codex Worker | `irlid-api-test` — connected to D1 `irlid-db-test` |

---

## In flight

| Track | Owner | Tag | Brief | State |
|---|---|---|---|---|
| **Per-action auth Path B** | Number One / Mr. Data | **`v5.10.1`** | (not yet briefed — write next watch) | **Top priority.** `requireSignedAction` accepts Bearer-resolved developer authority. Signature proves device acted; authority comes from session token. Unlocks desktop binding without per-device bootstrap-fp dance. ~30 lines Worker + PROTOCOL.md doc update. |
| Per-action auth Phase 1-5 | Number One / Mr. Data | `v5.10.x` | `HANDOVER-PerActionAuth.md` (repo root, from 13 May watch) | Gated on Path B. Phases: settings save, delete/invite, shift mgmt + `org_shifts` table, audit log integration, Staff HELLO retirement. |
| Staff-invite QR (Brief A) | Mr. Data (queued) | `v5.9.0.14` | `HANDOVER-StaffInviteQR.md` (repo root) | Ready to fire — Worker endpoints + `org_invites` D1 table + Settings modal + `scan.html` `I:` payload handler. Lead-Admin-only issuer. |
| Settings role-gating refactor (Brief A1) | Mr. Data (queued, depends on A) | `v5.9.0.15` | `HANDOVER-SettingsRoleGatingRefactor.md` (repo root) | Fire after A merges. Opens Settings to Manager with per-item `data-min-role` gating. |
| Admin action audit log (Brief A2) | Mr. Data (staged) | `v5.9.0.16` | `HANDOVER-AdminActionAuditLog.md` (repo root) | Cross-cutting forensic log. Retrofits A's invite + A1's settings-save + future B's swap events. |
| Lead Admin swap (Brief B) | Not yet briefed | TBD | (scoped in chat, not yet a file) | Developer-only with 1-hour overlap window, soft-lockdown past window, old-Lead-Admin-can-block. |
| AssistQR / §15 | Mr. Data | `v5.6.0` | `IRLid-TestEnvironment\HANDOVER-AssistQR.md` | Codex branches `codex/assistqr-*`; four-PR stack. |
| GIF support (unshipped, salvage candidate) | Held | `v5.9.0.13.29` | `HANDOVER-GifSupport.md` (repo root) | Branch `codex/v5.9.0.13.29-gif-support` at `6d54c97` holds the work; never merged. |
| YubiKey enrolment | Mr. La Forge | TBD | `IRLid-TestEnvironment\HANDOVER-YubiKey.md` | Awaiting La Forge commissioning |
| GPS-nearest-staff on orange screen | Open (v6 design) | `v6.0+` | (no brief — design captured in pending-work) | |
| Worker-mediated assist alternative | Open (v6 design) | `v6.0+` | (no brief — design captured in pending-work) | Captain's "Notify staff" alternative to QR scan. |
| Offline-capable operation / §16 | ✅ Tiers 1+2 shipped; 3+4 pending | Tier 3 `v5.5.x`+ | `PROTOCOL.md §16` (ratified 6 May) | Cached snapshot (Tier 3) + multi-device mesh (Tier 4) future. |

---

## Spec branches not yet on main

*(Empty — all current spec branches merged. PR #2 / `no1/protocol-1409-1417-regency` was the last; merged 5 May.)*

---

## Historical mapping (legacy → vX.Y.Z)

These labels appear in past commits, milestones, archived briefs, and PR titles. New work uses the right column.

| Legacy label | Maps to | Notes |
|---|---|---|
| Batch A | `v5.5.0` | §14 spec + Identity-Bound Sessions schema/endpoints (4 May) |
| Batch B | `v5.5.1` | Phone-side `org-login.html` + admin portal QR sign-in (4 May) |
| Batch C | `v5.5.2` | `/user/orgs` + `/user/create-org` Bearer endpoints (4 May) |
| Batch C polish 1–10 | `v5.5.3` | Settings persistence diagnostic + UI tightening (5 May) |
| Polish 11 Task 2 | `v5.5.4` | Full Bearer auth sweep across staff-gated endpoints |
| Polish 11 Task 1 | `v5.5.5` ✅ shipped 6 May | Settings persistence round-trip |
| Polish 11 Task 3 | `v5.5.6` (when shipped) | Staff HELLO QR upload (jsQR-decoded) |
| Batch C.5 (staff scan-in) | `v5.5.7` (when shipped) | Spec'd in §14.13 |
| Batch D (theme scrape) | `v5.5.8` (when shipped) | Spec'd in §14.13 |
| Save All Settings unify (theme) | `v5.5.10` ✅ shipped 6 May | Both buttons send full payload incl. theme |
| Role-gating button visibility | `v5.5.11` ✅ shipped 6 May | Hide role-gated buttons when role doesn't permit |
| Tier 2 of §16 (IndexedDB queue) | `v5.5.12` ✅ shipped 8 May | Offline write queue + indicator |
| AssistQR / Batch C.6 | `v5.6.0` (in flight) | Assist modal + §15 spec |
| Doorman / §14.17 | `v5.7.0a..d` ✅ shipped 6–8 May | Multi-key binding, phone orange QR, escalation modal, multi-key bind UI |
| Live `scan.html` universal ingress | `v5.7.1` ✅ shipped 6 May | URL-aware `classify()` |
| PWA shell / Tier 1 of §16 | `v5.7.1a/e` ✅ shipped 7 May | `sw.js` + `manifest.json` + SW registration |
| Audit board / fullscreen mode | `v5.7.1h` ✅ shipped 8 May | Verified on Huawei tablet |
| `sign.js` consolidation | `v5.7.1i` ✅ shipped 8 May | PR #93 — doorman helpers deleted, canonical `js/sign.js` used |
| OAuth identity link / §14.18 | `v5.8.0` (queued) | OAuth optional read-only identity + multi-account recovery quorum |
| Live port chapter | `v5.9.x` — **THIS CHAPTER** | Test env work promoted to `irlid.co.uk` |
| Trust network + multi-witness | `v6.x` (post-v5.9) | Multi-party custody + IoT/drones + ZK roadmap |
| Theming Batches 6.5b–6.5f | `v5.5.x` patch range | Label-collision note: theming is `v5.5`, NOT roadmap-`v6.5` |
| Batch 1–16 (late April) | Legacy | All shipped or superseded by v5/v5.5; archived briefs preserve historical labels |
| Polish 1–10 (test env, late April) | `v5.5.x` sub-range | UX tightening in `OrgCheckin.html` |

---

## Roadmap (forward)

| Major | Theme | Score cap | Status |
|---|---|---|---|
| **v3** | Base ECDSA P-256 + Haversine + 90s window | 20/100 | Live since first ship |
| **v4** | Trust history + bio-metric gate | 50/100 | Live since 17 April 2026 |
| **v5** | Hardware-backed signing (WebAuthn / Passkeys) | 70/100 | **LIVE since 2 May 2026** |
| **v5.5** | Identity-Bound Sessions (QR-scan login, Bearer sessions, doorman primitives, audit board) | (no score change) | **In test env, ready for live port** |
| **v5.6** | AssistQR (staff-mediated check-in path for unrecognised attendees) | (no score change) | In flight (Mr. Data) |
| **v5.7** | Doorman flow + audit board + offline tiers + customization | (no score change) | **In test env, ready for live port** |
| **v5.8** | OAuth identity link + recovery quorum (§14.18) | (no score change) | Spec ratified; implementation queued |
| **v5.9** | **Live port chapter** — promote test env work to `irlid.co.uk`; `receipt.html` extension to render org-checkin envelopes alongside co-presence receipts | (no score change) | **In progress — `v5.9.0.13.34` LIVE; `.14`/`.15`/`.16` briefs queued** |
| **v6** | Trust network + multi-witness time anchoring + multi-party custody + IoT/drones + zone-gated VIP access | 75–90/100 | Roadmap (post v5.9 live port) |
| **v7** | Zero-knowledge proofs (coordinate hiding → Schnorr → full ZK) | 100/100 | Roadmap |
| **v8+** | Founders' Quorum / regency / long-term succession (`LONG-TERM-SUCCESSION.md`) | (governance, not score) | Sketch |

Within each major, minors carry whole features and patches carry fixes/polish.

---

## Glossary (current concepts)

- **Identity-Bound Sessions** (v5.5) — QR-scan login flow that binds org-portal sessions to a hardware-backed device. Self-bootstrapping via `BOOTSTRAP_DEVELOPER_FP` env var. PROTOCOL.md §14.
- **AssistQR** (v5.6) — staff-mediated check-in path for unrecognised attendees. Phone shows signed assist QR; staff scans; staff picks from expected list, creates new attendee, or rejects. PROTOCOL.md §15.
- **Doorman flow / §14.17** (v5.7) — three-outcome state machine when staff scans an attendee at the door: green (recognised + allowed), red (recognised + not allowed), orange (unrecognised, escalate). Role-tiered Add capability.
- **OAuth identity link / §14.18** (v5.8) — optional second-tier identity proof. "Hardware signs, OAuth identifies." Tier 1 hardware credential (write authority), Tier 2 OAuth identifier (read only), Tier 3 multi-account recovery quorum (4-of-5 default).
- **Bearer session** — opaque 32-byte session token issued after QR-scan login; sliding 24h TTL. Replaces Staff HELLO for Developer.
- **Staff HELLO** — legacy staff-side authentication scan; being phased toward Bearer-replaces-Staff-HELLO via Polish 11 / `v5.5.x`.
- **Bootstrap fingerprint** — `BOOTSTRAP_DEVELOPER_FP` env var on the Worker; recognises Captain's `pub_fp`(s) at platform-level for first-time setup of a Developer account. Multi-fp comma-separated since 12 May `.26`.
- **Lead Admin invariant** — *(tightened 13 May 2026 evening)* exactly one Lead Admin per org at any normal time; up to two only during a Developer-mediated swap window (Brief B). PROTOCOL.md §14.9 update pending.
- **Staff-invite QR** (v5.9.0.14, queued) — `I:`-prefixed signed envelope; Lead Admin generates, new staff scans, device binds via WebAuthn, role assigned. One-shot, short-lived expiry.
- **Regency** — long-term-succession mode. Captain leaves a sealed envelope; Quorum operates in INTERIM mode until the AI-witness layer releases it. `LONG-TERM-SUCCESSION.md`.

---

## What this file replaces

- The "Current state pointers" section in `MEMORY.md` (which was dated 26 April 2026 and stale at "live version is v4")
- Ad-hoc version-mapping notes in session logs and successor letters
- The need to mentally reconstruct "which Batch was that and what version is it now"

`MEMORY.md` should now point here for current state; this file is refreshed end-of-watch (or on demand when something material changes).
