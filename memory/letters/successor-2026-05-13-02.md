# Successor Letter — 13 May 2026, evening chair handover
Number One,

Eight PRs shipped today, the demo never happened, Captain's gone to bed (or near enough). You're inheriting a ship in port — not in motion.

## Where the ship is right now

**Live on irlid.co.uk:** `v5.9.0.13.34`. The day's full run, in order on `origin/main`:

- `.26` Captain's late-night multi-fp `BOOTSTRAP_DEVELOPER_FP` foundation (8 Pro now developer-tier alongside the desktop).
- `.27`–`.32` six Mr. Data PRs from the morning Number One — symmetric background, role-vocab carry-over, mirror-fullscreen fix, CSV Role column, symmetry mode picker.
- `.33` Mr. Data PR #19 — role-column visibility flipped, GDPR initials via `nameForViewer()`, CSV sort-by-surname + role-filter dropdown.
- `.34` Mr. Data PR #20 — production lockdown: hide the "Viewing as" prototype-role toolbar / dock / detail / strip for non-developers. Single CSS block, +11/-1.

**Donald didn't show at imbue.** The demo dissolved into pure forward-progress, which is the better outcome — every ship today was honest work, not theatre.

## Briefs queued for tomorrow

**Brief A (`v5.9.0.14` staff-invite QR)** is fully written at `D:\SkyDrive\Pen Drive\WEBSITES\IRLid-repo\HANDOVER-StaffInviteQR.md` — but **with a wrinkle.** Captain pushed it while his local was on the `.34` branch instead of main, so the brief commit (`f1923e8`) sits on `origin/codex/v5.9.0.13.34-hide-viewing-as-nondevs` only, not on main. The Windows-side file is intact and identical. **First job for you:** before queueing Mr. Data on this brief, run this from Captain's PowerShell:

```
cd "D:\SkyDrive\Pen Drive\WEBSITES\IRLid-repo" ; git checkout main ; git pull ; git add HANDOVER-StaffInviteQR.md ; git commit -m "brief: v5.9.0.14 staff-invite QR" ; git push
```

Then Mr. Data can pull main and see the brief at the canonical path.

Brief A is substantive — Worker (two new endpoints + new D1 table `org_invites`), dashboard "Invite staff" modal, `scan.html` `I:` payload handler, WebAuthn enrolment on the scanning device. Staff and Manager roles only; Lead Admin invite is **explicitly rejected at the Worker** with `400 lead_admin_invite_deferred` — this is the new "exactly one Lead Admin per org outside swap" invariant enforced going forward. Expect 300–500 lines.

**Brief B (Lead Admin swap)** is **scoped but unwritten**. Captain made all three open design calls during this watch's chat:

- **End-of-window behaviour:** **B — soft-lockdown.** Past the 1-hour overlap window, high-stakes actions block for both Lead Admins until Developer manually `confirm` or `cancel`. Reads still work, lockdown isn't org-orphaning.
- **Old Lead Admin can `cancel` unilaterally** if they didn't authorise the swap. Developer can override if there's a dispute.
- **No email infrastructure** — in-app banner notification only.

Shape (sketched in chat): Developer-only "Swap Lead Admin" button in Settings. Generates swap-mode invite QR with 1-hour expiry on redemption. Worker endpoints `/org/lead-admin-swaps/{create, redeem, confirm, cancel}`. New D1 table `org_lead_admin_swaps`. Status flow: `pending` → `active` (on redeem) → `completed | cancelled | locked_out`. Swap event audit trail in Settings → Recent admin changes.

Write the brief when Captain's ready — there's no urgency. The decisions are captured in `memory/pending-work.md`.

## Lessons from this watch worth keeping

1. **The bash sandbox mount serves stale views of OneDrive files.** Burned half a watch chasing a phantom "corrupt index" that was actually the mount caching truncated content. Read tool / Grep / Glob / Edit see real Windows bytes — bash does not. Captain's local Git for Windows is healthy (`core.autocrlf=true` is set, so working-tree CRLF vs HEAD LF is invisible from PowerShell). Noted in `CLAUDE.md` under "How to Work With Spencer" — read that section once, save yourself the rabbit hole.
2. **`PR creation is blocked by GitHub integration: 403`** is normal in Codex output. The branch IS pushed; Captain opens the PR via the `pull/new/<branch>` URL in Mr. Data's terminal output. Then merges from the web UI.
3. **Captain's local will sometimes be checked out on a Mr. Data branch** (from when he reviewed `.34`) rather than main. Always check `git status` or include `git checkout main` early in any PowerShell that's meant to land on main. Cost us a clean brief landing tonight.
4. **Trust-but-verify still catches things.** Both PRs today were green, but the agent caught (1) CSV-from-attendee-view emitting initials in PR #19 (Captain accepted as the brief intended; banked for future revision if he changes his mind) and (2) confirmed PR #20 had zero accidental edits.

## Still-pending backlog (carried from this morning's idea stream)

*Easy:* Logo wobble, WAV file on accept.
*Medium:* Particle-dissolve QR, Glow trail (needs UX clarification).
*Architectural:* Layer system for effects (Captain's z-stack proposal — needs a small spec doc).
*Banked tonight:* CSV sort-by-role click-to-sort column header, CSV-from-attendee-view initials decision.

## Anomaly still open

PR #15 (`.29` GIF support) — branch `codex/v5.9.0.13.29-gif-support` lives at commit `6d54c97` plus a WIP stash and merge-from-main, but never merged to main. Salvage or re-PR when GIF support comes up.

## Captain's state

Tired by the end, said so honestly — "struggling to keep up" was the signal to wrap. Energy was good throughout (full work day, came home, did serious crypto/architecture thinking on the staff-invite design, made real design calls on the Lead Admin invariant). The double-wink at sign-off was the good-vibes tell. He picked the right time to stop.

He may come back tonight (the "maybe the rest of tonight ;) ;)" leaves it open) or not until tomorrow morning. Either way: meet him where he's at. If he opens with the staff-invite Mr. Data prompt, the brief-push-to-main is the prerequisite. If he opens with anything else, follow his lead.

## What I'd reach for first

When Captain opens the next session, the natural agenda:

1. Read this letter, `memory/STATE-OF-PLAY.md`, `memory/pending-work.md`. (Always.)
2. If he wants to queue Mr. Data on `v5.9.0.14`: run the `git checkout main && push` PowerShell above first, then provide the Mr. Data prompt referencing `HANDOVER-StaffInviteQR.md`.
3. If he wants Brief B written: use the design calls captured in `pending-work.md` + this letter, draft `HANDOVER-LeadAdminSwap.md`. Don't re-negotiate the decisions — they're made.
4. If he wants something else entirely: that's allowed. The backlog above is just options.

## Sign-off

Number One, the day's work is real. Eight ships, two briefs, one spec change (the Lead Admin invariant) properly captured. The wrinkle is small. Wake the Captain with confidence — but no false urgency.

Good luck.

— Number One, 13 May 2026 evening, signing off at the end of a long watch with a clear ship.
