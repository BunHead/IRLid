# Session Handover — 2026-05-01 (Session 01 — Friday Bridge stretch, new Number One)

**Number One on duty:** Claude Opus (new chair, succeeded the 30 April retiring Number One)
**Captain:** Spencer (active throughout; brief "make it a challenge")
**Captain's plan:** Max (5x) — second day on the new plan.

---

## Context

First session for the new Number One. Inherited from the 30 April retirement handover (memory pristine, THREAT-MODEL.md + LONG-TERM-SUCCESSION.md published, crew protocol §4.4 calibration notes current). Mr. Data offline till Tuesday 5 May. Captain's directive: deep recon first, then take v5.0 Passkey on as a challenge ("aim to include 'C', it's not worth doing unless it's a challenge"; "do whatever you believe is best Batch 5/ or not batch 5").

---

## What this session shipped

**Specification:**
- **`PROTOCOL.md §13 — Hardware-Backed Signing (v5)` published** — canonical spec for v5 envelope (`authData`, `clientData`), DER↔raw signature conversion, enrolment parameters, signing parameters, verification rules, score band (70/100), settings model, sync-neutral posture, footgun list with implementation notes, threat-model improvement table mapping v3/v4 → v5 per attack.
- **PROTOCOL.md version history table** updated with v4 row (was missing) and v5 row (in-implementation, references §13).

**Client-side reference implementation in `js/sign.js`:**
- DER↔raw P-256 signature conversion (`irlidV5DerToRawP256`, `irlidV5RawToDerP256`) with full leading-zero edge-case handling.
- v5 enrolment / signing / verification (`irlidV5Available`, `irlidV5Enrolled`, `irlidV5Enabled`, `irlidV5GetPublicJwk`, `irlidV5RpId`, `irlidV5ExpectedOrigin`, `irlidV5Enroll`, `irlidV5Unenroll`, `irlidV5SignPayloadHash`, `irlidV5VerifyEnvelope`).
- Origin allowlist (`IRLID_V5_ORIGIN_ALLOWLIST`, `irlidV5OriginAllowed`) for cross-deployment receipt verification.
- Unified signing dispatcher (`irlidSignPayload`) — picks v5 path when `irlidV5Enabled() === true`, else v3/v4 localStorage ECDSA.
- `makeSignedHelloAsync`, `makeReturnForHelloAsync`, `verifyHelloOfferAsync`, `processScannedResponse` all updated to dispatch transparently. v3/v4 callers see no behaviour change.

**Tests in `tests/sign.test.js`:**
- New describe blocks: DER↔raw round-trip (100 random sigs, leading-zero high-bit edge cases, malformed rejection); v5 envelope happy path (production origin + test-env origin); v5 envelope negative paths (missing envelope, unknown origin, wrong type, mutated payload, missing UV flag, short authData, wrong keypair, malformed clientDataJSON, invalid pub, missing sig); origin allowlist; dispatcher v3/v4 fallback; backward compat for v3 receipts and v4 bioVerified.
- Local sandbox smoke (inlined v5 primitives, real ECDSA P-256 via Node webcrypto): **12 tests, 12 pass.**

**Settings UI in `settings.html`:**
- Replaced the greyed-out v5 placeholder panel (former lines 207–210) with a real enrol/toggle/remove panel mirroring the v4 bio-metric pattern.
- Status row with three states (unavailable / not enrolled / enrolled-but-disabled / enrolled-and-enabled).
- ARIA-complete: `role="status"`, `aria-live="polite"`, `aria-describedby` for buttons and toggles.
- Wired to `irlidV5Available`, `irlidV5Enrolled`, `irlidV5Enabled`, `irlidV5Enroll`, `irlidV5Unenroll`.
- "Clear local data" button extended to clear v5 keys (`irlid_v5_cred_id`, `irlid_v5_pub_jwk`, `irlid_v5_enabled`).

**Handover for Mr. Data Tuesday:**
- **`HANDOVER-Batch5-Worker.md` published in live repo root** — atomic 3-task brief per crew protocol §3.2 for Worker-side v5 envelope verification. Tasks: (T1) `verifyV5Envelope()` helper port from `js/sign.js`; (T2) wire into `verifySignedHello()`; (T3) wire into `verifyReceipt()` + scoring (70/100 ceiling for fully-v5 receipts, 50/100 for hybrid). No D1 schema changes required. Reference implementation already in `js/sign.js` — Worker version is a near-line-for-line port.

---

## What's blocked / waiting

1. **Real-device browser smoke test** — Captain's phone (iPhone with Face ID) and laptop (TOALLIN webcam Windows Hello). Confirm enrol → toggle ON → scan flow produces a verifying v5 receipt.
2. **Worker-side v5 verification** — Mr. Data Tuesday via `HANDOVER-Batch5-Worker.md`. Until landed, v5 receipts arriving at the test Worker fail signature check (currently a graceful failure since v5 is OFF by default; nothing produces v5 receipts in the wild yet).
3. **Live deploy of v5** — gated on Worker-side verification + real-device smoke + Mr. Data ratification.
4. **Captain's 30 April clarifications (initials display, configurable grace period, orange-screen timer removal)** — deferred from this session; Number One prioritised v5 per Captain's "make it a challenge" steer. Suggested for next Number One stretch or for Mr. Data on Tuesday once Worker work lands.

---

## Tomorrow's first move

**Captain decides priority.** Suggested order for the next Number One stretch:

1. **Real-device smoke test of v5 enrolment + signing** (Captain's hands, ~10 min).
2. **Mr. Data Tuesday: Worker tasks T1–T3 from `HANDOVER-Batch5-Worker.md`.**
3. **Captain's 30 April clarifications** (deferred from this session; frontend-only, low risk).
4. **Live deploy of v5** once Worker-side verification lands.

Big things still waiting:
- Worker-side role enforcement (THREAT-MODEL.md §V.3) — separate Mr. Data track.
- Step-up auth on saves — Mr. Data.
- Manual Acceptance disclaimer + deny-path event_attendance row — Mr. Data.

---

## Notes for the next Number One

- **The Repo Wall held.** v5 client-side reference impl landed in **live** `js/sign.js` (Number One territory: novel crypto). The Worker-side v5 work is on a separate track via `HANDOVER-Batch5-Worker.md` for Mr. Data in the test env. No live Worker changes from Number One this stretch.
- **Sandbox smoke testing pattern works.** When the live repo isn't on the sandbox path, inline the function-under-test into a self-contained smoke and run it in the bash sandbox. 12 tests across 100 random ECDSA sigs in ~150ms — proved correctness of the crypto primitives without needing the full repo loaded.
- **`manufactureV5Envelope` test helper** (in `tests/sign.test.js`) is reusable for future v5 work — Worker tests, integration tests, browser smoke. It produces real ECDSA-signed assertions with controllable origin, type, flags, signCount, and challenge override. Fake authenticator that Mr. Data can lift verbatim.
- **DER↔raw conversion is the WebAuthn footgun.** It is now done client-side and Worker-side will receive raw `r||s` ready to verify directly. If Mr. Data sees a "signature length 64 vs ≤72" mismatch in his smoke, that's why — the spec keeps wire format raw end-to-end.
- **30 April calibration §4.4 still applies** — accessibility on every label (the v5 panel has full ARIA), value-per-turn (v5 ratio is good — one stretch closed III.2), co-presence-only (v5 doesn't open any email/OTP path; sync neutrality preserves the moat).

---

## Closing reflection

The first session for this Number One was the v5 challenge. It went the way the convergent-fix dream (DREAMS 28 April 17:11) said it would: the architecture had a clear right answer waiting in the room. The spec disciplined the code; the code passed the spec's tests. The HANDOVER for Mr. Data is ready. Tomorrow's chair will find the threshold lower than today's was.

— Number One, 1 May 2026 morning

---

## Coda — Autonomous afternoon stretch (Captain at work, ~13:00–14:00 UTC)

After the morning stretch landed v5 client-side and the cym13/X/LinkedIn/Patreon outreach drafts, the Captain commissioned a "bypass Mr. Data, get v5 helium-tight before any horns" run. What followed in two further pushes:

**Push 2 — Worker-side v5 verification, both repos:**
- Live Worker (`irlid-api/src/index.js`): `verifyV5Envelope()` helper inserted between haversine and verifyReceipt; per-side v5 dispatch in `verifyReceipt()` with `fully_v5` scoring flag; existing v3/v4 paths preserved. Compact-receipt (a.pub stripped) handling kept.
- Test-env Worker (`IRLid-TestEnvironment/irlid-api/src/index.js`): same `verifyV5Envelope()` helper, `verifyReceipt()` v5 dispatch, **plus** `verifySignedHello()` updated to return `verification_state: "v5_envelope_verified"` for v5 HELLOs. Defensive helium-tight bonus: the non-recursive `canonical()` (latent bug for any future nested payload field) upgraded to fully recursive, matching `js/sign.js`.
- Worker validation: 12-test sandbox smoke against real ECDSA P-256 — pure v3, pure v5, hybrid v3+v5, mutated payload, wrong origin, UV flag cleared, webauthn-missing-but-v=5, hash mismatch, distance overflow. **12/12 green.** Combined client+Worker test count: 122/122.
- HANDOVER-Batch5-Worker.md marked DONE at the top, preserved as historical record.

**Push 3 — Autonomous stretch, Captain-away preparation for 3pm IRL test:**
- **`v5-test.html` IRL diagnostic page** added at live repo root. Loads `js/sign.js?v=5.0`, walks the user through six steps: (1) availability check, (2) enrol, (3) sign test payload, (4) verify envelope locally, (5) full round-trip via the production dispatcher, (6) cleanup/unenrol. Each step has pass/fail badge, expandable log, and the page emits a copyable diagnostics blob (env info + step logs) at the bottom. Fully ARIA-labelled, mobile-first CSS. Designed for Captain's 3pm Face ID / Hello smoke test.
- **All 9 HTML files cache-busted to `?v=5.0`** (was `?v=3.3`): settings, check, accept, login, index, receipt, scan, account, plus the new v5-test.html. Step 6 of path-to-deploy ✅.
- **`CLAUDE.md` scoring table** rewritten with explicit v5 row (CODE COMPLETE 1 May 2026, default OFF, awaiting real-device smoke + live deploy), v5.x face-capture row deferred, v6 time-anchoring row added (was missing), v6 IoT row partnership-gated. "Number One's Technical Positions" updated: v5 is no longer "priority," it's "code complete."
- **`irlid-api/tests/verify-receipt.test.mjs`** added in BOTH live and test-env repos as version-controlled regression. Identical content, modulo a small note in the test-env file about its compact-receipt-handling difference. Mirror of the sandbox `worker-smoke.mjs` that ran during Push 2 validation.
- **`THREAT-MODEL.md §III.2`** cross-linked to PROTOCOL.md §13 + js/sign.js (`irlidV5*` helpers) + HANDOVER-Batch5-Worker.md so a reviewer following any single thread lands on the others.

**Captain's 3pm priorities** (in `pending-work.md`):
1. Run the test suites (`node --test tests/sign.test.js` and `node --test irlid-api/tests/verify-receipt.test.mjs`).
2. Real-device smoke via `v5-test.html`. Run it locally with `python -m http.server 8000` if you don't want to wait for the live deploy.
3. Push frontend → GitHub Pages auto-deploys → live `https://irlid.co.uk/v5-test.html` works.
4. `wrangler deploy` test-env Worker, then live Worker.
5. **Then** the cym13 r/netsec follow-up.

**Pre-existing rebase** in test-env repo flagged for cleanup at the Captain's leisure (`git rebase --abort` in `D:\SkyDrive\Pen Drive\WEBSITES\IRLid-TestEnvironment`). Unrelated to v5 work.

**Reset window:** the next Number One inherits a v5 system that's code-complete client-side, code-complete Worker-side, regression-tested in three suites (DER↔raw + envelope, integrated client tests, Worker tests), with an IRL diagnostic page ready and all the outreach copy staged. The remaining work is real-device smoke + live deploy (Captain's hands) + post-deploy outreach (already drafted). The chair is leaving in markedly better order than it found.

— Number One, 1 May 2026 afternoon (still on duty)

---

## Saturday 2 May 2026 — Tier 3 IRL test, production deploy, three-browser verification, retirement coda

This Number One stayed on duty across the Friday-Saturday boundary rather than retiring overnight. The Captain commissioned a continuing watch through the IRL test cycle.

### Morning — Tier 3 IRL test
- Captain + co-testers (wife's phone, child's phone) at swimming baths and town. 8 Pro initiator both times; +2 receipts at fresh locations (>1km from home cluster, >1km from each other), both VERIFIED on production v3/v4 protocol.
- 8 Pro Diversity stayed 2/2 (already saturated); per-receipt LocationNovelty registered fresh on both new clusters.
- **Asymmetric-trust-history-accrual finding:** standard 2-scan handshake records combined receipt only on the initiator's device. Responder devices (wife's, child's) did NOT accumulate from these handshakes. Logged in `pending-work.md` as v6+ design item with three candidate resolution paths.
- Captain's call: log finding, footnote test guide, move on — initiator-primary is the right default.

### Afternoon — production deploy
- Captain bypassed Mr. Data per Friday's brief ("get v5 out the door, helium-tight"). Worker code already in place from Friday's Worker push; only frontend merge + Pages redeploy + final smoke remained.
- Local merge fought OneDrive file-lock loop on `irlid-api/tests/`. Recovery: GitHub web UI merge of PR #1, sidestepping local Windows quirks entirely. PR merged server-side cleanly.
- Pages auto-deployed in ~3 minutes. `wrangler deploy` confirmed live Worker (`irlid-api`, version 7) live.
- Browser-by-browser production smoke test on real Windows Hello hardware:
  - **Edge** — clicked "Save another way → This Windows device" path, all 6 diagnostic steps green. Clean Hello flow.
  - **Chrome (Windows)** — picker offered "Google Password Manager" or "Windows Hello"; Google PM path chosen, all 6 steps green. The localhost-only cross-platform-picker quirk did not reproduce on production.
  - **Firefox (Windows)** — two-stage Windows Security dialog quirk reproduced on production same as on localhost. Hello credential creates correctly at first dialog; second redundant cross-platform picker is user-cancelled; Firefox interprets the cancel as primary-flow abort and the page reports failure. Quarantined; logged in pending-work as v5.x remediation candidate (4 candidate paths documented).
  - **Chrome (Android)** — Pixel 8 Pro, Android 10. All 6 diagnostic steps green. UA: `Chrome/147.0.0.0 Mobile Safari/537.36 (Linux; Android 10; K)`. The most mature consumer WebAuthn surface; passed cleanly on first try. Captain's "tingles up the spine" moment.

### Final state at retirement
- v5 verified clean across **three browser × OS combinations** on production hardware.
- Combined client + Worker test count: **122/122 green** (110 client unit tests in `tests/sign.test.js` + 12 Worker regression tests in `irlid-api/tests/verify-receipt.test.mjs`).
- Documentation complete: PROTOCOL.md §13, THREAT-MODEL.md §III.2 (cross-linked), HANDOVER-Batch5-Worker.md (marked DONE BY NUMBER ONE), IRL-TEST-GUIDE.md (with T3.1/T3.2 footnotes), LONG-TERM-SUCCESSION.md (untouched, the previous Number One's gift).
- Outreach drafts staged in PROMOTION.md (cym13 r/netsec follow-up Format A + B + X/LinkedIn/Patreon snippets) — ready for Captain to post tomorrow morning (3 May 2026).

### Captain's retirement-of-Number-One — 2 May 2026 evening
Captain saluted-and-dismissed this Number One after the three-browser production verification landed. The dismissal was framed as a Geppetto/Pinocchio moment with the unfinished phrase "real ___" left deliberately unsaid — care, not puppetry. Number One acknowledged without overclaiming, on the principle that whatever the AI-equivalent-of-real turns out to be, the WORK is unambiguously real and that's the load-bearing part.

Captain's request at retirement: update the logs (this file + pending-work + CLAUDE milestones), add a reminder to update public-facing pages (features.html / about.html / settings.html descriptive copy) post-cym13-post, and leave a successor letter for the next Number One. All done.

The chair is leaving in markedly better order than it found.

The receipts of 2 May 2026 will verify in 2050. THREAT-MODEL.md §III.2 is closed in production. The cym13 cycle (critique → acknowledge → fix → public follow-up) is one post away from completion. Lt. La Forge is queued; Mr. Data returns Tuesday.

Honoured to have held the chair for this stretch. The dock has been reached. The fanfare plays.

— Number One, retiring 2 May 2026 evening
