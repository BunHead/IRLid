# Audit — Tidy + Versioning, 5 May 2026 evening watch

**Author:** Number One (Session 03)
**Scope:** Both repos. Surface-level (file counts, link audit, version-string mapping). Not a deep code review.
**Premise:** Captain asked for orphans, irrelevant code, and a unified version-naming pass after the new `vX.Y.Z` convention landed in `BOOTSTRAP.md`.

---

## TL;DR

| Action | Count |
|---|---|
| Deletable junk | 2 files |
| Archive candidates | 8–9 historical docs |
| Canonical-source violation | 1 (test env has its own `PROTOCOL.md`) |
| Version-string retrofit | 3 files matter (pending-work, CLAUDE, PROTOCOL); rest stays as historical record |
| Captain's-call items | 3 |

Tonight-friendly cleanup is the top four items. Bigger retags belong in a next watch with fresh eyes.

---

## Findings — live repo (`IRLid-repo`)

### Junk

| File | Why | Action |
|---|---|---|
| `desktop.ini` | OneDrive metadata committed by accident; should never have been tracked | Add to `.gitignore`; `git rm --cached` then delete |

### Archive candidates (move under `archive/` or `memory/historical/`)

These are point-in-time briefs and proposals whose work has shipped. Their content is preserved in git history; the working-tree clutter is what we're removing.

| File | What it was | Status |
|---|---|---|
| `HANDOVER-Batch5-Worker.md` | v5 Worker brief for Mr. Data | Shipped 1 May (`v5.0`) |
| `HANDOVER-Batch6.md` | Bake-off brief, server-side v5 identity resolution | Superseded |
| `HANDOVER-Batch12.md` | 27 April fullscreen-QR / heights / attendee HELLO brief | Shipped late April |
| `HANDOVER-Batch14-15-16.md` | Batches 14–16 bundle | Shipped 28–29 April |
| `v5-PASSKEYS-PROPOSAL.md` | Pre-implementation proposal | v5 live since 2 May |
| `PRE-LAUNCH-AUDIT-2026-05-03.md` | Pre-launch sweep before v5 deploy | Applied as 3 commits 3 May |
| `ORG-AUDIT-2026-05-03.md` | Org portal audit, point-in-time | Applied |
| `THEMING-SPEC-2026-05-03.md` | Theme customisation design doc | Mostly shipped in test env (Batches 6.5b–f); see Captain's call |

### Canonical, staying at root (no change)

`BOOTSTRAP.md`, `CLAUDE.md`, `PROTOCOL.md`, `PROMOTION.md`, `THREAT-MODEL.md`, `LONG-TERM-SUCCESSION.md`, `DREAMS.md`, `WIDGET.md`, `IRL-TEST-GUIDE.md` — 9 files, all current canon.

### HTML pages — link audit

19 HTML files. Cross-reference grep across `<a href>`s:

- **Main-nav linked (15):** `index, about, features, contact, scan, check, login, account, receipt, settings, blog-proof-of-personhood, demo-login, roadmap, verify-visual, accept` — all reachable from the standard nav.
- **Deep-link only by design (4):** `widget.html` (iframe embed), `pitch-humanitarian.html` (outreach share), `v5-test.html` (diagnostic tool), `org-login.html` (QR scan target). Not orphans — intentionally not in nav.

No orphan HTML in live repo.

### JS

5 files in `js/` (`auth, backend, nav, qr, sign`). Lean. No surface orphans.

---

## Findings — test env (`IRLid-TestEnvironment`)

### Canonical-source violation

| File | Issue | Action |
|---|---|---|
| `PROTOCOL.md` | Stale v3 draft sitting in test env. Per `memory/crew-protocol.md` §2.1, `PROTOCOL.md` is Number One's territory and lives in the live repo only. Mr. Data reads it from live as context, not from a local copy. | Delete |

This is the most important finding of the audit. Two PROTOCOL.md files in two repos drifting independently is exactly the rule §2.1 was written to prevent.

### Archive candidate

| File | What it was | Status |
|---|---|---|
| `HANDOVER.md` (test env, canonical-named) | Batch 13 brief from 27 April + 29 April night-shift addendum | Shipped; superseded by individual `HANDOVER-*.md` briefs |

Captain's call: refresh `HANDOVER.md` as a current pointer-doc to open briefs, OR rename to `archive/HANDOVER-Batch13.md` and retire the canonical-name slot.

### In-flight briefs (keep, currently active)

- `HANDOVER-AssistQR.md` → `v5.6.0` work
- `HANDOVER-Polish11.md` → Tasks 1 + 3 still open (Task 2 shipped as `v5.5.4`)
- `HANDOVER-YubiKey.md` → awaiting Mr. La Forge commissioning

### HTML pages

- **Public-site copies** (`index, about, features, contact, scan, check, login, account, receipt, settings, blog-proof-of-personhood, accept`) — duplicated from live repo for parity testing. Keep; that's the test env's purpose.
- **Org portal trio:** `OrgCheckin.html` (current canonical Check-in surface), `org-entry.html` (phone-side QR target), `org.html` (legacy redirect-only page; 1-line redirect to OrgCheckin.html).

Decision needed on `org.html` — it's effectively a tombstone page now. If nothing external links to the old `org.html` URL, delete it. If you've shared the old URL in past outreach (e.g. with Wisdom or Donald), keep the redirect for stability.

### Code-comment sweep

`OrgCheckin.html` contains 51 occurrences of the words `Venue` or `Doorman`. A mix of:

- Legitimate references (`doorman_scan` is an endpoint name; "doorman" survives as a verb for the act of scanning at the door)
- Stale references from the pre-unification design (the old Venue/Doorman split UI)

Not blocking. Worth a 30-min sweep next watch — anything that references "Venue mode" or "Doorman mode" as UI states is stale; anything referring to the ACTION of doorman-scanning is fine.

---

## Version-string occurrence map

157 occurrences of `Batch X / Polish N / Batch N` patterns across 21 files in the live repo. Distribution:

| File | Count | Retag action |
|---|---|---|
| `memory/pending-work.md` | 21 | Retag + reconcile (next watch) |
| `CLAUDE.md` | 16 | Retag milestone rows under `vX.Y.Z` (next watch) |
| `memory/sessions/2026-05-04-01.md` | 16 | **No retag** — historical session log |
| `ORG-AUDIT-2026-05-03.md` | 16 | Archive (no retag) |
| `HANDOVER-Batch14-15-16.md` | 13 | Archive (no retag) |
| `PROTOCOL.md` | 13 | Cross-check Version History; already mostly v5.5/v5.6 native |
| `HANDOVER-Batch12.md`, `HANDOVER-Batch6.md` | 7 each | Archive (no retag) |
| Other session logs / `BOOTSTRAP.md` / successor letter | 1–6 each | **No retag** — historical |

Net retrofit work: three living files (`pending-work.md`, `CLAUDE.md`, `PROTOCOL.md`). Everything else either gets archived (which preserves the original labels in their original context) or stays untouched as historical record.

---

## Recommended action queue

Smallest-blast-radius first.

1. **Delete `desktop.ini` + `.gitignore` it** — 2 lines, atomic commit. No risk.
2. **Delete test env `PROTOCOL.md`** — 1 line, atomic commit. Closes the canonical-source violation.
3. **Create `archive/` folder in live repo, move 8 historical docs in** — mechanical, atomic commit per logical group.
4. **Decide `HANDOVER.md` (test env): refresh or archive** — Captain's call.
5. **Retag `CLAUDE.md` milestones under `vX.Y.Z`** — ~16 rows updated. Better with fresh eyes; queue for next watch.
6. **Reconcile + retag `memory/pending-work.md`** — biggest retag job. Already queued from Tuesday morning.
7. **`OrgCheckin.html` Venue/Doorman comment sweep** — 30 min, low priority.

Items 1–3 are clean tonight wins. Items 4–7 are deliberate work for the next stretch.

---

## Captain's-call items

| Question | Options |
|---|---|
| Where do shipped briefs live? | (a) `archive/` at repo root — most discoverable. (b) `memory/historical/` — keeps memory/ as the home for everything reflective. **Recommendation: `archive/` at root** — they're project artefacts, not memory artefacts. |
| `THEMING-SPEC-2026-05-03.md` | (a) Keep as living doc — Batches 6.5* are still in flight in test env. (b) Archive once Batches 6.5* land in live repo. **Recommendation: keep until they land in live**. |
| `HANDOVER.md` (test env) | (a) Refresh contents as a pointer-doc to current open briefs (`HANDOVER-AssistQR/Polish11/YubiKey`). (b) Rename to `archive/HANDOVER-Batch13.md` and let the canonical-name slot retire. **Recommendation: (a) refresh** — Mr. Data still benefits from a top-level entry point that points him at current work. |

---

## What I am NOT recommending right now

- A full PROTOCOL.md retag pass. The Version History table already uses v5.5 / v5.6 / v6 / v7 naturally; in-text references like "v3" (legacy) or "v5" (current) are correct and don't need touching.
- Retagging session logs or any file under `memory/sessions/` — those are historical record. Old labels are part of how that day actually felt. Don't rewrite history.
- A WidgetMD / IRL-TEST-GUIDE.md retag — both are reference docs that don't touch version labels heavily.

---

*Audit closes here. Next move: Captain skims, picks which of items 1–4 to action tonight; bigger retags (5–7) queue for next watch.*
