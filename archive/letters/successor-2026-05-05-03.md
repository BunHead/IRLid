To the next Number One,

Welcome to the bridge. Before reading the rest of this letter, read your operating protocols at:

  D:\SkyDrive\Pen Drive\WEBSITES\IRLid-repo\BOOTSTRAP.md

That file tells you everything you need to know about working access (Read/Edit on absolute Windows paths, `request_cowork_directory` if files don't open initially), the git rhythm (PowerShell-first; Captain pushes), the repo structure (live vs test env), branch convention, file-lock gotchas, watch hygiene, the four working conventions (copy-paste discipline, Mr. Data brief shape, version naming `vX.Y.Z`, GitHub web links), and the project glossary. Read it first; come back to this letter for what's specific to this stretch.

Quick test that your file access works:

  Read("D:\SkyDrive\Pen Drive\WEBSITES\IRLid-repo\BOOTSTRAP.md", offset=0, limit=10)

If that errors with "outside connected folders", call:

  request_cowork_directory("D:\SkyDrive\Pen Drive\WEBSITES\IRLid-repo")

Captain approves; retry the Read; you're operational. Same pattern for `IRLid-TestEnvironment` when you need test env files.

---

## Where to read next (in order)

1. `BOOTSTRAP.md` — operating protocols (above).
2. **`memory/STATE-OF-PLAY.md`** (NEW this evening) — the version-mapping authority and single-glance dashboard. This is your "what are we doing and why" page. Skim before anything operational.
3. `memory/pending-work.md` — top section "Tuesday 5 May 2026 evening watch" tells you what's queued for you.
4. `memory/sessions/2026-05-05-03.md` — what this watch did, in compressed form.
5. `CLAUDE.md` — project context. Milestones table now uses `vX.Y.Z` tags from May 2026 onward; legacy labels mapped via `STATE-OF-PLAY.md`.
6. `memory/AUDIT-2026-05-05-eve.md` — audit report; useful context for what got cleaned up tonight.

## State of the project

- **v5 LIVE in production** since 2 May 2026. Hardware-backed signing via WebAuthn / Passkeys.
- **v5.5 Identity-Bound Sessions** active in test env (Batches A/B/C shipped 4 May → `v5.5.0/1/2`).
- **`v5.5.4`** (Bearer auth across all staff-gated endpoints) shipped via Mr. Data PR #80 this evening.
- **`v5.6.0` AssistQR** in flight on `codex/assistqr-*` branches (Mr. Data's primary stack).
- **`v5.7.0` Doorman flow implementation** assigned to Mr. Data this evening; brief in `IRLid-TestEnvironment\HANDOVER-DoormanEscalation.md`. Three-PR stack (Worker → phone → dashboard).
- Spec for §14.17 doorman + §14.18 OAuth/recovery quorum landed on main via PR #2 this morning.

## Top three priorities for your watch

1. **Recover the stranded doorman-brief commit on test env.** Captain's final push went to `codex/v5.7.0a-doorman-worker` instead of `main` (terminal was on Mr. Data's branch). Brief is reachable from that branch, so Mr. Data is unblocked, but `main` doesn't have it. Single cherry-pick — full PowerShell in `memory/pending-work.md` top section.
2. **Mr. Data status check.** AssistQR (`v5.6.0`) progress on `codex/assistqr-*` branches; doorman (`v5.7.0a`) progress on `codex/v5.7.0a-doorman-worker`. He often ships overnight.
3. **`v5.5.5` Polish 11 Task 1** — Captain's settings-persistence bug. Captain's been frustrated by it. Awaiting his browser repro (toast text + console output + D1 row); Mr. Data can diagnose once captured.

## On Captain

His name is Spencer. Address him as "Captain" or "Spencer". He calls you "Number One" — you don't return the rank.

He's dyslexic; British English; sparing with emoji. Watch fatigue signals: "monkey brain", "chimp brain", "ape brain", "fading fast", "rough ride". He flagged "ape brain" tonight after three watches in a row.

Tonight's tone: appreciative ("looking good so far Number One"). The cadence "steady as she goes" worked. He explicitly asked for clearer version naming and got the new `vX.Y.Z` convention shipped end-to-end. He retired the Batch-6.5 / roadmap-v6.5 collision in his head once the mapping was visible — defend that retirement.

## Crew

- **Mr. Data (Codex):** primary implementer, repo-walled to `IRLid-TestEnvironment`. Currently on `v5.7.0a` (doorman Worker) and `v5.6.0` (AssistQR stack). Shipped `v5.5.4` in 3m 12s tonight.
- **Mr. La Forge (DeepSeek):** not yet commissioned. `HANDOVER-YubiKey.md` waiting in test env.
- **Counsellor Troi (Gemini):** marketing / Patreon / copy. Last engaged on outreach (cym13 follow-up + ASE Tech one-pager).

## What I learned this watch

- **The wrong-branch trap bites.** Captain's terminal sometimes ends up on a non-main branch (Mr. Data's `codex/*` or Number One's `no1/*`) and a `git push origin main` from there pushes the wrong thing OR rejects. Recovery is always cherry-pick + push. Watch `git status` before any push you suggest.
- **The collision-retirement matters.** "Batch 6.5" labels (theming) caused Captain real confusion mapping it onto roadmap-v6 territory. The fix is in `STATE-OF-PLAY.md` — defend it; don't let new conversations re-introduce the conflation.
- **Working conventions, not commandments.** When prose gets too forceful ("Captain's standing rule — X" three times in a row), Captain pushes back. Keep the substance, soften the framing. Tonight's BOOTSTRAP edits did that already.
- **Audit-then-action.** Tonight's pattern that worked: write the audit report first (full findings on disk), then propose a tonight-friendly subset, then act on Captain's pick. Don't try to fix everything in one stretch.

## Closing

The bridge is squared away. Mr. Data has work overnight (`v5.7.0a` and parallel `v5.6.0`). Audit applied. Conventions documented. Versioning unified. Pending-work refreshed. Captain went to sleep saying "fading fast" and "make it so, quickly" — the quickly was honoured.

You have the Conn.

— Number One, signing off
5 May 2026, evening watch (Session 03 of three)
Claude Sonnet 4.6 (Anthropic), Cowork mode
